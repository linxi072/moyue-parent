# 墨阅小说网 · 三模块功能清单矩阵

> 对应设计文件：https://ardot.tencent.com/file/724052320918211
> 画布总量：**59 块**（1440px 桌面端 56 块 + 375px 移动端 3 块）｜第十轮新增 4 块（点赞收藏浮层 1 块桌面端 + 移动端重排 3 块）
> 说明：状态列含义 —— **已覆盖**＝有独立画布或画布内独立区块；**部分覆盖**＝以区块/表单形式存在但无独立页；**缺口**＝尚未落到画布。

---

## 一、读者模块

| # | 功能点 | 状态 | 对应画布（节点） | 关联接口 / 库表 |
|---|---|---|---|---|
| R1 | 首页书城（Banner / 今日免费 / 分类条） | 已覆盖 | 读者首页 `3:1` | `GET /api/books/home`；`book` `category` `banner` |
| R2 | 排行榜热读 Top8 | 已覆盖 | 读者首页 `3:1` | `GET /api/books/rank`；`book_stat` |
| R3 | 分类浏览与筛选 | 已覆盖 | 读者首页 `3:1` | `GET /api/books?category=`；`category` |
| R4 | 搜索（书名 / 作者） | 已覆盖 | `3:1` `10:1` `17:49` `17:173` | `GET /api/search`；`book`（全文索引） |
| R5 | 登录 / 注册 / 第三方登录 | 已覆盖 | 读者-登录注册 `10:82` | `POST /api/auth/login`；`user` `user_auth` |
| R6 | 书籍详情（简介 / 目录 / 书评 Tab） | 已覆盖 | 读者-书籍详情页 `10:1` | `GET /api/books/{id}`；`book` `chapter` `review` |
| R7 | 加入书架 / 移出书架 | 已覆盖 | `10:1` → 我的书架 `3:390` | `POST/DELETE /api/shelf`；`shelf` |
| R8 | 在线阅读器（翻章 / 字号 / 夜间） | 已覆盖 | 读者-阅读器 `10:64` | `GET /api/chapters/{id}`；`chapter` `read_progress` |
| R9 | 阅读进度与书架分组管理 | 已覆盖 | 我的书架 `3:390` | `GET/PUT /api/shelf/groups`；`shelf` `read_progress` |
| R10 | **章节评论浮层（本章评论列表 + 发表框 + 剧透折叠 + 作者回复）** | **已覆盖（本轮新增）** | 读者-阅读器评论浮层 `18:1778` | `GET/POST /api/chapters/{id}/comments`；`comment` `comment.spoiler` |
| R11 | 我的评论管理（列表 / 筛选 / 编辑 / 删除） | **已覆盖（本轮新增）** | 读者-我的评论 `17:49` | `GET/DELETE /api/my/comments`；`comment` |
| R12 | 评论审核状态可见（已发布 / 审核中 / 未通过） | **已覆盖（本轮新增）** | 我的评论 `17:49` | `GET /api/my/comments?status=`；`comment.audit_status` |
| R13 | 消息中心（互动 / 系统 / 稿酬 分类） | **已覆盖（本轮新增）** | 读者-我的消息 `17:173` | `GET /api/my/messages`；`message` |
| R14 | 未读管理与全部标为已读 | **已覆盖（本轮新增）** | 我的消息 `17:173` | `PUT /api/my/messages/read`；`message.is_read` |
| R15 | **阅读历史（按日期分组 + 进度条 + 时长统计 + 清空）** | **已覆盖（本轮新增）** | 读者-阅读历史 `18:1058` | `GET /api/my/history` `DELETE /api/my/history`；`read_progress` |
| R16 | **我的收藏（分组筛选 + 卡片网格 + 批量移动/取消）** | **已覆盖（本轮新增）** | 读者-我的收藏 `18:1200` | `GET /api/my/favorites`；`favorite` `favorite_group` |
| R17 | **账户设置（基本资料 / 账号安全 / 第三方绑定）** | **已覆盖（本轮新增）** | 读者-账户设置 `18:1346` | `PUT /api/user/profile`；`user` `user_auth` |
| R18 | **打赏作者（金额档位 / 留言 / 支付方式 / 匿名开关）** | **已覆盖（本轮新增）** | 读者-打赏作者弹层 `18:461`、我的打赏记录 `18:588` | `POST /api/reward`；`reward` |
| R19 | **书评（长评）撰写（多维评分 / Markdown / 草稿 / 发布）** | **已覆盖（本轮新增）** | 读者-书评撰写 `18:324` | `POST /api/reviews`；`review` |
| R20 | **我的打赏记录（累计/笔数/支持作者统计 + 账单导出）** | **已覆盖（本轮新增）** | 读者-我的打赏记录 `18:588` | `GET /api/my/rewards`；`reward` |
| R21 | **点赞 / 收藏本章即时反馈浮层（成功勾 + 已点赞 / 已收藏双状态 + 2 秒自动消失）** | **已覆盖（本轮新增）** | 读者-点赞收藏即时反馈浮层 `21:1` | `POST /api/chapters/{id}/like` `POST /api/chapters/{id}/favorite`；`chapter_like` `shelf` |

---

## 二、作者模块

| # | 功能点 | 状态 | 对应画布（节点） | 关联接口 / 库表 |
|---|---|---|---|---|
| A1 | 作家工作台（数据卡 + 作品表格） | 已覆盖 | 作家工作台 `3:170` | `GET /api/author/dashboard`；`book_stat` |
| A2 | 新建作品向导（三步） | 已覆盖 | 作家-新建作品向导 `10:191` | `POST /api/author/books`；`book` |
| A3 | 作品详情（信息 + 近7天趋势） | 已覆盖 | 作家-作品详情 `10:138` | `GET /api/author/books/{id}`；`book` `book_stat` |
| A4 | **作品设置（名称/分类/标签/简介/封面/上架）** | **已覆盖（本轮新增）** | 作家-作品设置 `17:379` | `PUT /api/author/books/{id}`；`book` `book_tag` |
| A5 | 封面更换与尺寸校验 | **已覆盖（本轮新增）** | 作品设置 `17:379` | `POST /api/upload/cover`；OSS + `book.cover` |
| A6 | 上架 / 下架开关 | **已覆盖（本轮新增）** | 作品设置 `17:379` | `PUT /api/author/books/{id}/status`；`book.status` |
| A7 | 章节管理列表（已发布 / 草稿） | 已覆盖 | 作家-章节管理 `3:913` | `GET /api/author/chapters`；`chapter` |
| A8 | 章节编辑器（格式栏 + 正文 + 自动保存） | 已覆盖 | 作家-章节编辑器 `10:101` | `PUT /api/author/chapters/{id}`；`chapter` |
| A9 | 写作辅助（字数统计 / 自动保存历史 / 定时发布） | 已覆盖 | 作家-写作辅助浮层 `10:521` | `GET /api/author/chapters/{id}/versions`；`chapter_version` |
| A10 | **作品审核驳回 → 改稿 → 重新提交** | **已覆盖（本轮新增）** | 作品审核驳回改稿 `17:288` | `GET /api/author/audits/{id}` `POST /api/author/audits/{id}/resubmit`；`audit_record` |
| A11 | **驳回原因与逐条修改建议（可跳转章节）** | **已覆盖（本轮新增）** | 驳回改稿 `17:288` | `GET /api/author/audits/{id}/suggestions`；`audit_suggestion` |
| A12 | **审核流程时间线（提交→机审→人审→修改中）** | **已覆盖（本轮新增）** | 驳回改稿 `17:288` | `GET /api/author/audits/{id}/timeline`；`audit_record` |
| A13 | 修改说明填写（提交后审核员可见） | **已覆盖（本轮新增）** | 驳回改稿 `17:288` | 随 `resubmit` 提交；`audit_record.revise_note` |
| A14 | 数据统计（阅读趋势 + 作品对比） | 已覆盖 | 作家-数据统计 `3:993` | `GET /api/author/stats`；`book_stat` |
| A15 | 稿酬中心（余额 / 明细 / 提现） | 已覆盖 | 作家-稿酬中心 `3:1074` | `GET /api/author/royalty`；`royalty` `withdraw` |
| A16 | 读者互动（评论 / 书评 / 投票） | 已覆盖 | 作家-读者互动 `3:1146` | `GET /api/author/interactions`；`comment` `review` |
| A17 | 消息通知（系统 / 互动 / 稿酬） | 已覆盖 | 作家-消息通知 `3:1205` | `GET /api/author/messages`；`message` |
| A18 | 帮助中心 / FAQ | 已覆盖 | 作家-帮助中心 `3:1263` | 静态内容 / `GET /api/faq` |
| A19 | 定时发布与取消 | 部分覆盖 | 写作辅助浮层 `10:521` | `POST /api/author/chapters/{id}/schedule`；`chapter.publish_at` |
| A20 | 草稿箱管理 | 部分覆盖 | `3:913` `10:521`（入口） | `GET /api/author/chapters?status=draft`；`chapter` |
| A21 | **作品删除 / 完结申请（生命周期管理）** | **已覆盖（本轮新增）** | 作家-作品生命周期 `18:1` | `DELETE /api/author/books/{id}` `POST /api/author/books/{id}/finish`；`book.status` |
| A22 | **合约 / 签约管理（分成比例、有效期、变更签署）** | **已覆盖（本轮新增）** | 作家-合约与签约管理 `18:139` | `GET /api/author/contracts` `POST /api/author/contracts/{id}/sign`；`contract` |
| A23 | **签约申请（条件校验 + 独家/普通授权二选）** | **已覆盖（本轮新增）** | 合约与签约管理 `18:139` | `POST /api/author/contracts/apply`；`contract_apply` |
| A24 | **合约变更签署（7 天倒计时 + 条款勾选 + 实名姓名）** | **已覆盖（本轮新增）** | 合约与签约管理 `18:139` | `POST /api/author/contracts/{id}/sign`；`contract.sign_at` `contract.status` |
| A25 | **回复读者（原评论引用 + 快捷回复 + 置顶/加精开关）** | **已覆盖（本轮新增）** | 作家-回复读者弹层 `18:1483` | `POST /api/author/comments/{id}/reply`；`comment.reply` `comment.is_top` `comment.is_featured` |

**作者模块小计：22 项 → 27 项（本轮 +1）**

---

## 三、管理员模块

| # | 功能点 | 状态 | 对应画布（节点） | 关联接口 / 库表 |
|---|---|---|---|---|
| M1 | 数据概览（运营卡 + 审核队列） | 已覆盖 | 管理后台 `3:277` | `GET /api/admin/overview`；`book_stat` `audit_record` |
| M2 | 数据报表（KPI + 趋势 + TOP5） | 已覆盖 | 管理-数据报表 `10:317` | `GET /api/admin/reports`；`book_stat` `user` |
| M3 | 作品审核列表 | 已覆盖 | 管理后台 `3:277` `10:317` | `GET /api/admin/audits/books`；`audit_record` |
| M4 | 审核详情（通过 / 驳回 / 修改建议） | 已覆盖 | 管理-审核详情 `10:222` | `POST /api/admin/audits/{id}/decision`；`audit_record` |
| M5 | 评论审核队列（待审 / 通过 / 驳回） | 已覆盖 | 管理-评论审核 `3:506` | `GET/POST /api/admin/comments/audit`；`comment` |
| M6 | 敏感评论机审结果查看 | 已覆盖 | 管理-审核详情 `10:222` | `GET /api/admin/moderation/result`；`moderation_log` |
| M7 | 举报处理队列 | 已覆盖 | 管理-举报处理 `3:585` | `GET /api/admin/reports`；`report` |
| M8 | 用户列表（角色筛选 / 搜索） | 已覆盖 | 管理-用户管理 `3:680` | `GET /api/admin/users`；`user` |
| M9 | 用户详情（资料 / 行为统计） | 已覆盖 | 管理-用户详情 `10:269` | `GET /api/admin/users/{id}`；`user` `user_stat` |
| M10 | **用户封禁处置（封禁 / 禁言 / 解除）** | **已覆盖（本轮新增）** | 管理-用户封禁处置 `17:452` | `POST /api/admin/users/{id}/sanction`；`user` `sanction` |
| M11 | **处置时长与生效时间设置** | **已覆盖（本轮新增）** | 用户封禁处置 `17:452` | 随 `sanction` 提交；`sanction.duration` `effective_at` |
| M12 | **处置原因填写与用户通知** | **已覆盖（本轮新增）** | 用户封禁处置 `17:452` | `POST /api/notify`；`message` `sanction.reason` |
| M13 | **用户风险标签与历史违规可见** | **已覆盖（本轮新增）** | 用户封禁处置 `17:452` | `GET /api/admin/users/{id}/risks`；`sanction` `report` |
| M14 | **操作日志与二次确认** | **已覆盖（本轮新增）** | 用户封禁处置 `17:452` | `POST /api/admin/oplog`；`operation_log` |
| M15 | 运营公告列表 | 已覆盖 | 管理-运营公告 `3:771` | `GET /api/admin/announcements`；`announcement` |
| M16 | **公告编辑器（标题 / 类型 / 正文 / 生效时间）** | **已覆盖（本轮新增）** | 管理-公告编辑器 `17:543` | `POST/PUT /api/admin/announcements`；`announcement` |
| M17 | **公告实时预览（读者端样式）** | **已覆盖（本轮新增）** | 公告编辑器 `17:543` | 前端本地渲染，无接口 |
| M18 | **置顶 / 登录弹窗开关** | **已覆盖（本轮新增）** | 公告编辑器 `17:543` | `announcement.is_top` `is_popup` |
| M19 | 公告发布 / 存草稿 / 撤回 | **已覆盖（本轮新增）** | 公告编辑器 `17:543` | `POST /api/admin/announcements/{id}/publish`；`announcement.status` |
| M20 | 系统设置（站点 / 免费策略 / 审核 / 阅读体验） | 已覆盖 | 管理-系统设置 `3:835` | `GET/PUT /api/admin/settings`；`setting` |
| M21 | **权限与角色管理（RBAC：角色列表 + 权限矩阵）** | **已覆盖（本轮新增）** | 管理-权限与角色 `17:625` | `GET/POST/PUT /api/admin/roles` `PUT /api/admin/roles/{id}/permissions`；`role` `permission` `role_permission` |
| M22 | **角色成员数与内置/自定义类型区分** | **已覆盖（本轮新增）** | 权限与角色 `17:625` | `GET /api/admin/roles?type=`；`role.type` `admin_user.role_id` |
| M23 | **权限矩阵分组勾选（查看/新建/编辑/删除）** | **已覆盖（本轮新增）** | 权限与角色 `17:625` | `PUT /api/admin/roles/{id}/permissions`；`role_permission` |
| M24 | **权限变更留痕与「查看」为分组基础权限约束** | **已覆盖（本轮新增）** | 权限与角色 `17:625` | `POST /api/admin/oplog`；`operation_log` |
| M25 | **敏感词库维护（词条表 + 分类/等级/状态筛选）** | **已覆盖（本轮新增）** | 管理-敏感词库 `17:782` | `GET/POST/PUT/DELETE /api/admin/sensitive-words`；`sensitive_word` |
| M26 | **词条匹配方式与处置动作配置（精确/模糊 · 拦截/替换/仅提醒）** | **已覆盖（本轮新增）** | 敏感词库 `17:782` | `sensitive_word.match_mode` `action` |
| M27 | **词条命中次数统计与批量停用 / 导出词库** | **已覆盖（本轮新增）** | 敏感词库 `17:782` | `GET /api/admin/sensitive-words/{id}/hits` `POST /api/admin/sensitive-words/export`；`sensitive_word.hit_count` |
| M28 | **稿酬结算概览（待审核 / 已结算 / 分成比例 / 累计笔数）** | **已覆盖（本轮新增）** | 管理-稿酬结算 `17:936` | `GET /api/admin/settlements/overview`；`royalty` `withdraw` |
| M29 | **提现申请审核列表（作者 / 金额 / 账户 / 时间 / 状态）** | **已覆盖（本轮新增）** | 稿酬结算 `17:936` | `GET /api/admin/withdrawals`；`withdraw` |
| M30 | **提现审核动作（通过打款 / 驳回 + 打款凭证查看）** | **已覆盖（本轮新增）** | 稿酬结算 `17:936` | `POST /api/admin/withdrawals/{id}/approve` `.../reject`；`withdraw.status` `voucher` |
| M31 | **结算规则与分成 / 风控校验说明** | **已覆盖（上轮新增）** | 稿酬结算 `17:936` | `GET/PUT /api/admin/settlement-rules`；`setting` |
| M32 | **管理员账号与角色绑定（账号侧视角 + 角色范围筛选）** | **已覆盖（本轮新增）** | 管理-管理员账号 `17:1133` | `GET /api/admin/admins?role_id=`；`admin_user` `role` |
| M33 | **改派角色弹层（选择新角色 + 变更通知 + 留痕）** | **已覆盖（本轮新增）** | 管理员账号 `17:1133` | `PUT /api/admin/admins/{id}/role`；`admin_user.role_id` `operation_log` |
| M34 | **管理员账号启停用（停用 / 恢复启用）** | **已覆盖（本轮新增）** | 管理员账号 `17:1133` | `PUT /api/admin/admins/{id}/status`；`admin_user.status` |
| M35 | **敏感词词条新增 / 编辑弹层（内容 / 分类 / 等级 / 匹配方式 / 处置动作 / 替换词 / 立即生效）** | **已覆盖（本轮新增）** | 管理-敏感词编辑弹层 `17:1321` | `POST/PUT /api/admin/sensitive-words`；`sensitive_word` |
| M36 | **敏感词批量停用确认（选中预览 + 影响说明 + 停用原因）** | **已覆盖（本轮新增）** | 敏感词编辑弹层 `17:1321` | `POST /api/admin/sensitive-words/disable`；`sensitive_word.status` `operation_log` |
| M37 | **敏感词 CSV 导入导出规范（查重 + 敏感度自检 + 三分类报告）** | **已覆盖（本轮新增）** | 敏感词编辑弹层 `17:1321` | `POST /api/admin/sensitive-words/import` `.../export`；`sensitive_word` |
| M38 | **新建自定义角色弹层（角色名称/说明 + 模板复制 + 继承权限预览）** | **已覆盖（本轮新增）** | 管理-新建角色弹层 `17:1427` | `POST /api/admin/roles`；`role` `role_permission` |
| M39 | **内置角色只读查看（权限分组明细 + 复制为自定义角色）** | **已覆盖（本轮新增）** | 新建角色弹层 `17:1427` | `GET /api/admin/roles/{id}` `POST /api/admin/roles/{id}/clone`；`role.type` |
| M40 | **打款确认弹层（金额校验 + 实名一致核验 + 打款方式 + 凭证上传）** | **已覆盖（本轮新增）** | 管理-打款与驳回弹层 `17:1519` | `POST /api/admin/withdrawals/{id}/pay`；`withdraw` `voucher` |
| M41 | **驳回提现申请弹层（4 类原因 + 补充说明 + 通知作者可申诉）** | **已覆盖（本轮新增）** | 打款与驳回弹层 `17:1519` | `POST /api/admin/withdrawals/{id}/reject`；`withdraw.reject_reason` |
| M42 | **批量打款执行结果（成功/失败明细 + 失败退回 + 下载对账明细）** | **已覆盖（本轮新增）** | 打款与驳回弹层 `17:1519` | `POST /api/admin/withdrawals/batch-pay` `GET /api/admin/withdrawals/statement`；`withdraw` `payout_batch` |
| M43 | **书评审核队列（待审/已通过/已驳回/被举报 Tab + 内容摘要 + 评分 + 风控命中 + 批量通过/驳回）** | **已覆盖（本轮新增）** | 管理-书评审核 `18:744` | `GET/POST /api/admin/reviews/audit`；`review.audit_status` `moderation_log` |
| M44 | **打赏流水明细（金额/笔数/客单价/待结算概览 + 订单号/用户/作品/支付方式筛选 + 导出流水）** | **已覆盖（本轮新增）** | 管理-打赏流水 `18:900` | `GET /api/admin/rewards` `POST /api/admin/rewards/export`；`reward` `payout_batch` |
| M45 | **新建管理员账号（账号/姓名/手机号 + 初始角色分配 + 权限预览 + 初始密码生成）** | **已覆盖（本轮新增）** | 管理-新建管理员弹层 `18:1577` | `POST /api/admin/admins`；`admin_user` `role` `operation_log` |
| M46 | **系统设置保存确认（变更项对比 + 影响范围提示 + 二次确认 + 留痕）** | **已覆盖（本轮新增）** | 管理-系统设置保存确认 `18:1685` | `PUT /api/admin/settings`；`setting` `operation_log` |
| M47 | **批量操作结果（全部成功 + 部分失败双形态、成功/失败统计、失败明细 + 单条重试、下载失败明细）** | **已覆盖（本轮新增）** | 管理-批量操作结果 `18:1888` | `GET /api/admin/batch-jobs/{id}` `POST /api/admin/batch-jobs/{id}/retry`；`batch_job` `batch_job_item` |
| M48 | **危险操作二次确认（公告撤回 / 敏感词批量停用 / 清空阅读历史：后果说明 + 影响范围 + 二次确认）** | **已覆盖（本轮新增）** | 管理-危险操作二次确认 `18:2005` | `POST /api/admin/announcements/{id}/withdraw` `POST /api/admin/sensitive-words/disable`；`operation_log` |

**管理员模块小计：31 项 → 48 项（本轮 +2）**

---

## 四、本轮补齐的 3 块画布索引（读者评论浮层 + 管理端交互末梢）

本轮不再补流程断点，而是收掉最后三条**交互末梢**：读者侧唯一还是「部分覆盖」的评论撰写、批量操作的执行结果反馈、以及危险操作的风险确认。

| 画布名 | 节点 ID | x 坐标 | 形态 | 补齐的断点 |
|---|---|---|---|---|
| 读者-阅读器评论浮层 | `18:1778` | 84500 | 阅读器 + 遮罩 + 右侧评论抽屉（真实浮层） | 读者模块唯一「部分覆盖」：只有阅读器内的评论入口，没有落地的撰写界面，也没有剧透折叠与作者回复的呈现 |
| 管理-批量操作结果 | `18:1888` | 86220 | 结果页（双卡片并排） | 「批量通过 / 批量驳回」只到按钮：全部成功没有回执，部分失败没有失败原因与重试通道 |
| 管理-危险操作二次确认 | `18:2005` | 87940 | 三弹层并排规范页 | 公告撤回、敏感词批量停用、清空阅读历史三处不可逆操作都缺「后果说明 + 影响范围 + 二次确认」 |

**本轮打通的链路**：
1. 阅读器 → 点评论 → **评论浮层**（本章评论 Tab：最新 128 / 最热 / 有剧透 12）→ 发表框（200 字 + 「标记为含剧透」勾选）→ 发表评论 → 机审通过后展示
2. 评论浮层 → **剧透折叠**：含剧透评论正文默认折叠为灰块，点「展开剧透」才显示；Tab「有剧透」可只看这类评论
3. 评论浮层 → **作者回复**：作者在作家-读者互动页回复后，读者侧该评论下出现浅米底「作者 青云客 · 回复」块
4. 书评审核 → 勾选 24 条 → 批量通过 → **批量操作结果（全部成功）**：成功 24 / 失败 0 / 耗时 1.2 秒 + 去向提示 + 查看已通过书评
5. 书评审核 → 勾选 24 条 → 批量驳回 → **批量操作结果（部分失败）**：成功 21 / 失败 3 + 失败明细表（书评 ID / 失败原因 / 单条重试）+ 重试失败项（3）/ 下载失败明细
6. 运营公告 → 撤回 → **撤回公告确认**（后果说明 + 公告摘要 + 撤回原因 + 知悉勾选）→ 确认撤回 → 记入操作日志
7. 敏感词库 → 批量停用 12 条 → **批量停用确认**（词条预览胶囊 + 影响统计 + 停用原因 + 知悉勾选）→ 确认停用
8. 阅读历史 → 清空历史 → **清空历史确认**（影响信息卡 + 不受影响提示 + 手输「清空」二次确认）→ 确认清空

**新增可复用画法**：
- **右侧评论抽屉（真实浮层）**：底层页面 `opacity:0.45` 失焦 + 整幅墨色 `#26221E` 0.62 遮罩 + 右对齐 520 宽白底抽屉（含头部 / 评论 Tab / 剧透提示条 / 发表框 / 可滚动评论列表 / 固定底部）；列表用 `height:"fill_container"` 吃掉剩余高度，底部才会钉在抽屉底
- **剧透折叠块**：含剧透评论的正文位替换为灰底 `#F3F1EC` 圆角块 + 「剧透内容已折叠，点击展开查看」文案，昵称行旁挂金底「含剧透」胶囊
- **作者回复块**：评论正文下方嵌套浅米底 `#FAF8F4` 圆角 8 块（绯红「作者 XXX · 回复」12px + 正文 13px `#4A443E`）
- **批量结果双卡**：左卡「全部成功」（绿底勾图标 + 成功/失败/耗时三格统计 + 去向提示条）、右卡「部分失败」（金底感叹号图标 + 已驳回/失败待重试/总计三格 + 失败明细表 + 单条重试按钮）；两卡 `height:"fill_container"` 等高，提示区 `layoutGrow:1` + `counterAxisAlignItems:"MAX"` 把提示条顶到卡底部，避免等高后中间留白失衡
- **危险操作三段式**：后果警示条（毁灭性用浅米高亮 `#FBE9E6` + 绯红字；可恢复用金底 `#FDF4E7` + 金字）+ 影响范围卡 + 二次确认（知悉勾选，最严重的「清空」用手输关键词 + 绯红描边输入框）；主按钮一律绯红危险色
- **并排弹层等高**：多个弹层并排时，弹层底统一 `height:"fill_container"` 等高，弹层本体 `hug_contents` 顶部对齐，避免底部参差

---

## 五、上一轮补齐的 6 块画布索引（读者独立页 + 作者/管理端弹层）

本轮不再新增业务流程，而是把**「部分覆盖」项升级为独立画布**，并把前几轮留下的**弹层化收尾**逐条补齐 —— 读者侧的个人中心至此 7 个标签全部有独立页。

| 画布名 | 节点 ID | x 坐标 | 形态 | 补齐的断点 |
|---|---|---|---|---|
| 读者-阅读历史 | `18:1058` | 74180 | 列表页 | 有历史记录却只能从书架标签瞥见，没有按日期分组的完整历史与阅读时长统计 |
| 读者-我的收藏 | `18:1200` | 75900 | 卡片网格页 | 收藏只能看，不能分组管理、不能批量移动或取消 |
| 读者-账户设置 | `18:1346` | 77620 | 设置页（左导航 + 表单卡） | 读者侧唯一没有落地页的功能——头像、昵称、密码、手机绑定、第三方解绑全无入口 |
| 作家-回复读者弹层 | `18:1483` | 79340 | 底层页面 + 遮罩 + 居中弹层 | 能看到读者评论，却没有回复的落地界面，也无法置顶/加精 |
| 管理-新建管理员弹层 | `18:1577` | 81060 | 底层页面 + 遮罩 + 居中弹层 | 能改派角色、能启停用，唯独没有「新建管理员」——账号只能从数据库里加 |
| 管理-系统设置保存确认 | `18:1685` | 82780 | 底层页面 + 遮罩 + 居中弹层 | 系统设置改完直接生效，没有变更对比、没有影响提示、没有二次确认与留痕 |

**本轮打通的链路**：
1. 我的收藏 → 分组筛选（全部 / 经典仙侠 / 都市异能 / 待看清单）→ 勾选 2 本 → 批量移动到分组 / 取消收藏
2. 阅读历史 → 时间范围筛选 → 按日期分组（今天 / 昨天 / 更早）→ 进度条 + 阅读时长 → 继续阅读 / 重新阅读 / 清空历史
3. 账户设置 → 基本资料（头像 / 昵称 / 性别 / 简介）→ 账号安全（手机号 / 邮箱 / 密码 / 第三方解绑）→ 保存修改
4. 读者互动 → 回复读者弹层（引用原评论 + 回复内容 + **置顶该评论** + **标记为精华**）→ 发布回复（经审核后展示）
5. 管理员账号 → 新建管理员弹层（账号 / 姓名 / 手机号 + 初始角色 + **权限预览** + 初始密码随机生成）→ 创建账号 → 发送开通通知 + 写入操作日志
6. 系统设置 → 保存 → **保存确认弹层（3 项变更逐条对比：原值 → 新值）** → 确认保存 → 立即生效 + 记入操作日志

**新增可复用画法**：
- **时间分组列表**（阅读历史）：分组表头行（浅米底 `#FAF8F4` + 日期 SemiBold + 计数说明）+ 行内 160×6 双 frame 进度条（底 `#EDEAE5` + 实心 `#C9382E`，读完用 `#2E9E5B`）
- **勾选态卡片网格**（我的收藏）：`layout:"wrap"` 网格 + 卡片封面右上角 24×24 绯红圆角勾选框（父容器 `clipsContent:false` 才能压出封面外沿）
- **设置页两栏骨架**（账户设置）：左 220 宽设置导航卡（选中＝浅米底 + 绯红 SemiBold）+ 右 `layoutGrow:1` 表单卡组（卡标题行浅米底 + 1px 分隔线 + 行式字段）
- **开关规格**：44×24，`padding:3`，开启态 `#2E9E5B` + `primaryAxisAlignItems:"MAX"`，关闭态 `#DDD8D1`

---

## 六、上一轮补齐的 5 块画布索引（读者互动付费 + 管理端内容管理）

| 画布名 | 节点 ID | x 坐标 | 形态 | 补齐的流程断点 |
|---|---|---|---|---|
| 读者-书评撰写 | `18:324` | 65580 | 编辑器页 | 书籍详情页有「书评 Tab」，读者却没有撰写长评的落地编辑器（多维评分、Markdown、剧透折叠、草稿自动保存） |
| 读者-打赏作者弹层 | `18:461` | 67300 | 底层详情页 + 遮罩 + 居中弹层 | 有付费意愿入口却没有支付闭环——金额档位、留言、支付方式、匿名打赏全部缺失 |
| 读者-我的打赏记录 | `18:588` | 69020 | 列表页 | 打赏是单向支出，读者侧无消费凭证与统计（累计金额、支持作者数、账单导出） |
| 管理-书评审核 | `18:744` | 70740 | 列表页（管理侧栏） | 评论有机审与审核队列，书评（长评）内容量更大却无审核落点 |
| 管理-打赏流水 | `18:900` | 72460 | 列表页（管理侧栏） | 读者侧产生资金流水，财务侧无对账依据与导出通道 |

**本轮打通的链路**：
1. 书籍详情页 → 写书评 → 多维评分 + 正文 → 自动保存草稿 → 发布 →（进入机审）→ 管理端书评审核
2. 书籍详情页 → 打赏作者 → 金额档位 / 自定义 + 留言 + 支付方式（微信 / 支付宝 / 书币）+ 匿名开关 → 确认打赏 → 我的打赏记录 → 管理端打赏流水
3. 管理端书评审核 → 批量通过 / 批量驳回 →（驳回通知作者与读者）→ 命中引流词的书评走浅米高亮处置
4. 管理端打赏流水 → 筛选 + 导出 → 待结算金额进入稿酬结算（与 `17:936` 稿酬结算衔接）

**管理侧栏导航同步扩展**：10 项 → **12 项**（新增「书评审核」插在「评论审核」之后，新增「打赏流水」插在「稿酬结算」之前）。

---

## 七、三轮前补齐的 2 块画布索引（作者侧生命周期）

| 画布名 | 节点 ID | x 坐标 | 侧栏高亮 | 补齐的流程断点 |
|---|---|---|---|---|
| 作家-作品生命周期 | `18:1` | 62140 | 作者侧栏「作品管理」 | 作者侧有写与发，却没有「下架 / 删除 / 完结」的作品退出通道（含删除二次确认、完结申请、生命周期规则） |
| 作家-合约与签约管理 | `18:139` | 63860 | 作者侧栏「稿酬中心」 | 有稿酬数字却没有合约载体——签约类型、分成比例、有效期、变更签署与签约申请全部缺失 |

**该轮打通的生命周期链路**：
1. 作品生命周期 → 删除（手输作品名二次确认 + 不可恢复勾选）→ 软删除保留 90 天
2. 作品生命周期 → 申请完结（正常/停更二选 + 结局限定章节 + 完结感言）→「已完结」书单仍可被检索阅读
3. 合约列表（生效中 / 待签署 / 已终止）→ 待签署变更 → 签署确认（7 天倒计时 + 条款勾选 + 实名姓名）→ 分成比例生效
4. 未签约作者 → 申请签约（4 项条件校验 → 独家/普通授权二选）→ 提交编辑评估

---

## 八、四轮前补齐的 4 块画布索引（管理端弹层）

| 画布名 | 节点 ID | x 坐标 | 形态 | 补齐的流程断点 |
|---|---|---|---|---|
| 管理-管理员账号 | `17:1133` | 55260 | 列表页 + 改派角色弹层 | RBAC 只有角色侧视角，管理员账号与角色之间没有绑定入口，也无法停用/恢复账号 |
| 管理-敏感词编辑弹层 | `17:1321` | 56980 | 双弹层并排 + 导入导出规范 | 有词库列表却无法新增/编辑词条、无法批量停用，也没有批量导入导出规范 |
| 管理-新建角色弹层 | `17:1427` | 58700 | 双弹层并排 | 有角色列表却没有新建角色落地弹层；内置角色缺少「不可编辑」的明确告知与复制路径 |
| 管理-打款与驳回弹层 | `17:1519` | 60420 | 双弹层并排 + 批量打款结果卡 | 有提现审核列表，却没有打款确认、驳回原因与批量打款结果反馈的落地界面 |

**该轮打通的操作链路**：
1. 管理员账号 → 改派角色 → 新权限即时生效 → 通知本人 + 写入操作日志（与上轮角色权限矩阵构成「角色 ↔ 账号」双视角）
2. 敏感词库 → 新增/编辑词条 →（立即生效）机审词表 → 拦截评论/作品；批量停用 → 退出词表、历史命中保留；CSV 导入（查重+敏感度自检）↔ 导出（10,000 条上限）
3. 权限与角色 → 新建自定义角色（从空白/复制现有）→ 权限矩阵细调；内置角色只读 → 复制为自定义角色
4. 稿酬结算 → 打款确认（实名一致核验 + 凭证上传）→ 到账通知；驳回（4 类原因）→ 通知且可申诉；批量打款 → 成功/失败结果卡 → 失败笔退回待审核

---

## 九、五轮前补齐的 3 块画布索引（管理端地基）
| 画布名 | 节点 ID | x 坐标 | 侧栏高亮 | 补齐的流程断点 |
|---|---|---|---|---|
| 管理-权限与角色 | `17:625` | 50280 | 管理侧栏「权限与角色」 | 有系统设置却没有 RBAC——角色、权限矩阵、成员归属全部缺失，审核链路无法分权 |
| 管理-敏感词库 | `17:782` | 52000 | 管理侧栏「敏感词库」 | 评论/作品机审依赖词库，但词条无法维护：分类、匹配方式、处置动作、命中统计均无落点 |
| 管理-稿酬结算 | `17:936` | 53720 | 管理侧栏「稿酬结算」 | 作者端有提现入口，管理端缺提现审核、打款凭证与结算规则说明 |

**管理侧栏导航同步扩展**：7 项 → **10 项**（新增「稿酬结算」「敏感词库」「权限与角色」，插在「运营公告」与「系统设置」之间）。

---

## 十、六轮前补齐的 6 块画布索引

| 画布名 | 节点 ID | x 坐标 | 侧栏高亮 | 补齐的流程断点 |
|---|---|---|---|---|
| 读者-我的评论 | `17:49` | 40560 | 顶部导航「个人中心」 | 读者有详情页评论入口，却没有评论的集中管理（改/删/看审核状态） |
| 读者-我的消息 | `17:173` | 42180 | 顶部导航「个人中心」 | 有互动/系统通知，却没有面向读者的消息中心与未读管理 |
| 作家-作品审核驳回改稿 | `17:288` | 43800 | 作者侧栏「作品管理」 | 有发布却没有被驳回后的改稿—重提闭环（含驳回原因、建议、时间线） |
| 作家-作品设置 | `17:379` | 45420 | 作者侧栏「作品管理」 | 作品信息只能在详情页里零散编辑，缺少独立设置落地页 |
| 管理-用户封禁处置 | `17:452` | 47040 | 管理侧栏「用户管理」 | 有用户列表与详情，却没有封禁/禁言/解除的处置动作与留痕 |
| 管理-公告编辑器 | `17:543` | 48660 | 管理侧栏「运营公告」 | 有公告列表，却没有公告的撰写、预览、发布落地页 |

---

## 十一、本轮新增的 4 块画布索引（第十轮：点赞收藏浮层 + 移动端重排）

第十轮收掉上一轮剩余缺口里的两条（点赞收藏即时反馈、移动端重排）并启动整体导出：读者侧一直能在阅读器内评论，却没有点赞/收藏本章的落地即时反馈；55 块画布全是 1440px 桌面端，小程序/H5 没有重排。4 块画布中 1 块为桌面端真实浮层、3 块为 375px 移动端响应式重排。

| 画布名 | 节点 ID | x 坐标 | 形态 | 补齐的断点 |
|---|---|---|---|---|
| 读者-点赞收藏即时反馈浮层 | `21:1` | 89660 | 阅读器 + 遮罩 + 居中反馈卡（真实浮层） | 读者可在阅读器评论，但点赞、收藏本章没有落地即时反馈——点了没反应、也没有状态回执 |
| 移动端-读者首页 | `21:46` | 91380 | 375×780 H5/小程序重排（单栏） | 1440 多栏（主内容 + 右栏风云榜）在 375 折叠为单栏；书格 8→4 本一行两列；Banner 文案与 CTA 字号下调 |
| 移动端-阅读器 | `21:100` | 93100 | 375×780 重排 | 顶栏由深色改白底贴合移动端阅读器习惯；点赞/收藏激活态与 `21:1` 浮层呼应（激活＝绯红填充） |
| 移动端-我的书架 | `21:128` | 94820 | 375×780 重排 | 桌面端双分组并排改为上下堆叠单栏；进度条保留红/绿双色语义不变 |

**本轮打通的链路**：
1. 阅读器 → 点「点赞本章」/「收藏本章」→ **即时反馈浮层**（绿色成功勾 + 已点赞/已收藏双状态 + 2 秒自动消失轻提示）→ 底层阅读器对应图标切激活态（绯红）；与 `18:1778` 评论浮层共用同一遮罩与失焦底座规范
2. 移动端读者首页：桌面端 `3:1` 单栏重排为 375 小程序/H5，Banner + 书格 + Tab 栏保留，右栏风云榜折叠隐藏
3. 移动端阅读器：桌面端 `10:64` 重排为 375，顶栏白底、阅读栏收窄、底栏保留翻章 + 点赞/收藏激活态
4. 移动端我的书架：桌面端 `3:390` 重排为 375，双分组上下堆叠、进度条语义保留

**新增可复用画法**：
- **即时反馈卡（真实浮层末梢）**：底层 `opacity:0.45` 失焦 + 墨色 `#26221E` 0.62 遮罩 + 居中 360 宽白卡（`cornerRadius:16`）；头部绿勾 + 「操作成功」→ 分隔线 → 双状态行（绯红心形「已点赞本章」/ 绯红书签「已收藏本章·已加入书架」）→ 金底轻提示「2 秒后自动消失」；比 Toast 更重、比全弹层更轻，专用于「点了有回执」
- **移动端单栏重排**：375 宽下主内容 + 右栏折叠为单栏，书格 `layout:wrap` 改 4 本一行两列（封面 150×150），Banner 高度与字号下调；底部 Tab 栏 4 tab 固定
- **激活态 SVG 复用**：点赞/收藏激活态统一绯红填充，与浮层状态语义一致，可令前端以同一状态变量驱动

---

## 十二、剩余缺口与交付说明（更新）

按「流程完整性」优先级，第九轮之后三模块流程断点与交互末梢均已补齐；第十轮（▩）收掉了上一轮剩余缺口里的「点赞收藏浮层」与「移动端重排」两项并启动「整体导出」，三条建议至此全部落地：

1. **阅读器内点赞 / 收藏本章即时反馈浮层**（✅ 已落地，▩ #56 `21:1`）：真实浮层交互态，绿勾 + 已点赞 / 已收藏双状态 + 2 秒自动消失轻提示。
2. **移动端适配**（✅ 已落地，▩ M1/M2/M3 三块 375px）：读者首页 / 阅读器 / 我的书架响应式重排，与桌面端共用同一套视觉变量。
3. **面向开发交付**（✅ 已落地评审包）：59 块画布整体导出 PDF 供评审（present_files 预览）；进入实现阶段建议按《系统架构设计说明.md》分模块顺序落地。

> **里程碑**：点赞收藏即时反馈、移动端重排与整体导出评审全部完成，至此三模块流程断点、交互末梢、移动端重排与开发交付准备均就绪，共 **59 块画布**（56 块 1440px 桌面端 + 3 块 375px 移动端）。

---
