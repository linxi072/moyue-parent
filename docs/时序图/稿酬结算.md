# 时序图 · 稿酬结算

> 模块：稿酬 = `moyue-commerce` 的 `com.moyue.author`（作者稿酬查询桩） + `moyue-system` 的 `com.moyue.operation`（打赏支付与稿酬分成实际落库）。
> 阅读说明：**蓝色实线框内的调用链是已读源码确认的真实代码路径**；**红色虚线框内为"作者主动发起结算"的目标流程，当前代码尚未实现，属设计假设**（详见文末 Caption）。

## 真实路径一：作者查询稿酬流水（moyue-commerce `com.moyue.author`）

```mermaid
sequenceDiagram
    autonumber
    participant C as 作者客户端
    participant GW as 网关 moyue-gateway
    participant AC as AuthorController<br/>(moyue-commerce)
    participant AS as AuthorService
    participant AIM as AuthorIncomeMapper
    participant DB as author_income 表

    C->>GW: GET /api/v1/author/income/{authorId}
    GW->>AC: 路由 lb://moyue-commerce → AuthorController.listIncome(authorId)
    AC->>AS: listByAuthor(authorId)
    AS->>AIM: selectList(ΛQ.eq(author_id,authorId).orderByDesc(settle_month))
    AIM->>DB: SELECT * FROM author_income WHERE author_id=? ORDER BY settle_month DESC
    DB-->>AIM: List<AuthorIncomeEntity>
    AIM-->>AS: 流水列表(incomeType:1订阅/2打赏分成/3全勤奖)
    AS-->>AC: R<List<AuthorIncomeEntity>>
    AC-->>GW: R.ok(...)
    GW-->>C: 稿酬流水 JSON
```

> 真实方法名（已核实）：`AuthorController.listIncome(@PathVariable Long authorId)` → `AuthorService.listByAuthor(Long)` → `AuthorIncomeMapper.selectList(Wrappers.lambdaQuery().eq(AuthorIncomeEntity::getAuthorId, authorId).orderByDesc(AuthorIncomeEntity::getSettleMonth))`。

## 真实路径二：打赏支付触发稿酬分成结算（moyue-system `com.moyue.operation`）

```mermaid
sequenceDiagram
    autonumber
    participant R as 读者客户端
    participant GW as 网关 moyue-gateway
    participant RC as RewardController<br/>(moyue-system:8086)
    participant RS as RewardService
    participant ROM as RewardOrderMapper
    participant BC as BookClient<br/>(Feign→moyue-content)
    participant BK as BookController<br/>(moyue-content)
    participant AIM as AuthorIncomeMapper
    participant DB as author_income 表

    R->>GW: POST /api/v1/rewards  (bookId,chapterId,amount,payChannel)
    GW->>RC: createOrder(userId,X-User-Id,...)  [网关注入 X-User-Id]
    RC->>RS: createOrder(userId,bookId,chapterId,amount,payChannel)
    RS->>ROM: insert(reward_order, status=0 待支付, orderNo="RW"+ts+rand)
    ROM-->>RS: 已落库 RewardOrderEntity
    RS-->>RC: R.ok(order)
    RC-->>GW: 待支付订单
    GW-->>R: 订单(待支付)

    R->>GW: POST /api/v1/rewards/{orderNo}/pay
    GW->>RC: pay(orderNo, userId)
    RC->>RS: pay(orderNo, userId)  [幂等:已支付直接返回]
    RS->>ROM: updateById(reward_order, status=1 已支付, payTime=now)
    RS->>RS: settleAuthorIncome(order) 生成作者稿酬分成
    RS->>BC: getBook(bookId)
    BC->>GW: GET /api/v1/books/{bookId} (Feign name=moyue-content)
    GW->>BK: 路由 → BookController.getBook(bookId)
    BK-->>BC: R<BookSummaryDTO>(含 authorId)
    BC-->>RS: book.authorId
    RS->>AIM: insert(author_income: authorId,bookId,orderNo,<br/>incomeType=2 打赏分成, amount=order.amount×0.70,<br/>settleMonth=yyyy-MM)
    AIM->>DB: INSERT INTO author_income(...)
    DB-->>AIM: 已落库
    AIM-->>RS: 稿酬流水
    RS-->>RC: R.ok(order)
    RC-->>GW: 已支付订单
    GW-->>R: 支付成功(已生成稿酬分成)
```

> 真实方法名（已核实）：
> - `RewardController.create(@RequestBody CreateRewardRequest, HttpServletRequest)` → `RewardService.createOrder(Long userId, Long bookId, Long chapterId, BigDecimal amount, Integer payChannel)`
> - `RewardController.pay(@PathVariable orderNo, HttpServletRequest)` → `RewardService.pay(String orderNo, Long userId)` → 内部 `settleAuthorIncome(RewardOrderEntity)` → `resolveAuthorId(Long bookId)` 经 `BookClient.getBook(bookId)`（Feign `@FeignClient(name="moyue-content")`）→ `AuthorIncomeMapper.insert(...)`。
> - 分成规则：作者分成比例 `AUTHOR_SHARE_RATIO = 0.70`；`incomeType=2` 为打赏分成；`orderNo` 同时是支付幂等键。
> - **跨服务调用（真实 Feign）**：`RewardService` → `BookClient.getBook` → `moyue-content` `BookController.getBook`。这是本链路唯一的跨服务调用。

## 设计假设路径三：作者主动发起结算单 + 打款（当前未实现）

> 以下为**目标态设计假设**：当前 `com.moyue.author` 仅是查询桩，`moyue-system.operation` 也只对"打赏分成"自动落库，**不存在**"订阅分成 / 买断 / 结算单 / 待结算→已结算→已打款 状态机 / 调支付或账户打款"代码。该段仅用于对齐后续实现，方法名为建议命名。

```mermaid
sequenceDiagram
    autonumber
    participant A as 作者客户端
    participant GW as 网关 moyue-gateway
    participant SC as AuthorSettlementController (假设)
    participant SS as SettlementService (假设)
    participant UC as UserClient (Feign→moyue-account)
    participant BC as BookClient (Feign→moyue-content)
    participant AIM as AuthorIncomeMapper
    participant PAY as 支付/账户服务 (假设)
    participant DB as settlement_order / author_income 表

    A->>GW: POST /api/v1/author/settlement (authorId, settleMonth)
    GW->>SC: initiate(authorId, settleMonth)
    SC->>SS: compute(authorId, settleMonth)
    SS->>BC: getBook / 订阅数据校验作品有效性
    BC-->>SS: 作品/订阅数据
    SS->>AIM: 汇总 author_income(incomeType 1订阅/2打赏/3全勤)
    AIM-->>SS: 各项稿酬明细
    SS->>SS: 计算稿酬 = 订阅分成 + 打赏分成 + 买断(如有)
    SS->>DB: 生成结算单 settlement_order(status=待结算)
    SS->>PAY: 调支付/账户服务打款(authorId, amount)  [Feign→account/commerce]
    PAY-->>SS: 打款结果
    SS->>DB: 状态流转 待结算→已结算→已打款
    SS-->>SC: 结算单
    SC-->>GW: R.ok(结算结果)
    GW-->>A: 结算完成
```

---

### Caption（关键说明 / 假设）

1. **真实代码边界**：本仓库"稿酬结算"的真实落库仅发生在 `moyue-system.com.moyue.operation.RewardService.settleAuthorIncome`——**且仅覆盖"打赏分成（incomeType=2，比例 70%）"**，由读者打赏支付成功时自动触发。订阅分成、买断、作者主动发起结算单、打款状态机（待结算/已结算/已打款）**均未实现**。
2. **两张 `AuthorIncomeEntity`**：`moyue-commerce.com.moyue.author.entity.AuthorIncomeEntity` 与 `moyue-system.com.moyue.operation.entity.AuthorIncomeEntity` 都映射同一张 `author_income` 表——前者只读（查询桩 `AuthorController`），后者（在 operation 包）负责写（打赏分成落库）。这是跨服务的**共享表**依赖，非 Feign 调用。
3. **跨服务调用（真实）**：仅 `RewardService → BookClient.getBook(bookId)`（Feign，name=`moyue-content`）用于解析书籍作者 ID。commerce 的 `com.moyue.author` 模块**未引入** `moyue-api-account`/`moyue-api-content`，因此"调用支付/账户"在真实代码中并不存在（属假设路径三）。
4. **userId 来源**：打赏与查询均取网关注入的 `X-User-Id` 请求头（`Constants.USER_ID_HEADER`），不信任请求体，与全站约定一致。
5. **假设路径三**的方法名（AuthorSettlementController / SettlementService / settlement_order 表 / 调支付或账户服务）为建议命名，待后续需求确认后实现。
