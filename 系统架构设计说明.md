# 墨阅小说网 · 系统架构设计说明

> 交付日期：2026-09-10（含 09-10 晚补充）　｜　设计文件：`https://ardot.tencent.com/file/724375479120225`  
> 技术基线：**前端 Vue 3 ＋ 后端 Java / Spring Boot 3 · Redis · MySQL**

## TL;DR

为「墨阅小说网」输出了完整的**技术架构设计**，共 **8 块画布**（1440px）：系统总体架构图、后端框架结构（Spring Boot）、前端框架结构（Vue 3）、部署与运维架构、核心业务链路，以及补充的 **接口契约（OpenAPI）**、**数据库设计（ER + 表结构 + 完整 DDL）**、**核心时序图（登录鉴权 / 打赏支付 / 章节审核与自动发布 / 敏感评论机审）**。设计稿作为后续代码工程落地的蓝图，与既有 26 块界面设计稿（`file/724052320918211`）配套使用。

**本轮（文档交付轮）新增**：① 8 块画布导出 **PDF（合并，8 页）+ 8 张 2x PNG**；② 数据库页扩出 **MySQL 8 完整 DDL**（8 张表，落盘 `moyue-schema.sql`）；③ 接口页补 **Swagger / OpenAPI YAML 快照**（落盘 `openapi.yaml`，21 个路径 · 6 个分组 · 4 个 Schema，已通过 YAML 语法校验）；④ 时序图由 2 块扩为 **4 块**；⑤ 界面稿「状态与反馈规范」画布中 `✓ ✕ ◌ ↓ ▼ ←  → ↑` 等禁用字符**全部替换为独立 SVG 图标节点**。

---

## 一、画布清单

| # | 画布                     | 位置 (x) | 核心内容                                           |
| - | ---------------------- | ------ | ---------------------------------------------- |
| 1 | **系统总体架构图**            | 0      | 六层架构：客户端层 / 接入层 / 应用服务层 / 中间件层 / 数据层 / 基础设施层   |
| 2 | **后端框架结构**             | 1560   | 技术选型 · 分层架构（Controller→Service→Mapper）· 关键设计要点 |
| 3 | **前端框架结构**             | 3120   | 技术选型 · 工程目录结构 · 前端工程约定                         |
| 4 | **部署与运维架构**            | 4680   | 部署拓扑（6 段链路）· 环境与发布流水线 · 监控与运维                  |
| 5 | **核心业务链路**             | 6240   | 读者阅读链路 / 作者发布章节链路（各 6 步）                       |
| 6 | **★ 接口契约 OpenAPI 3.0** | 7800   | 契约约定 · 20 个核心接口清单 · 统一响应体 · 8 条错误码 · **Swagger YAML 快照** |
| 7 | **★ 数据库设计 ER**         | 9360   | 6 张核心表字段 · 实体关系 · 索引与规范 · **完整 MySQL DDL（6 张表）**    |
| 8 | **★ 核心时序图**            | 10920  | 登录鉴权 · 打赏支付 · **章节审核与自动发布** · **敏感评论机审**（各 12 步）    |

---

## 二、系统总体架构（六层）

| 层         | 职责                | 关键组件                                                                                    |
| --------- | ----------------- | --------------------------------------------------------------------------------------- |
| **客户端层**  | 多端接入              | 读者端 Web（Vue 3）· 移动端 H5 · 微信小程序 · 作家工作台 · 管理后台                                           |
| **接入层**   | 流量入口与安全           | Nginx 负载均衡 · CDN 加速 · Spring Cloud Gateway · JWT 统一鉴权 · Sentinel 限流熔断 · WebSocket 推送    |
| **应用服务层** | 业务能力（Spring Boot） | 用户 / 书城 / 阅读 / 作家 / 章节 / 互动 / 审核 / 运营 / 消息 / 统计 共 10 个服务                                |
| **中间件层**  | 能力复用              | Redis 分布式缓存 · RocketMQ 消息队列 · Elasticsearch 全文检索 · Nacos 注册与配置中心 · XXL-Job 定时任务         |
| **数据层**   | 持久化               | MySQL 8 主从集群 · Redis Cluster · MinIO / OSS 对象存储 · MongoDB 评论归档 · 定时备份容灾                 |
| **基础设施层** | 运行与观测             | Docker + Kubernetes · Jenkins CI/CD · Prometheus + Grafana · ELK 日志中心 · SkyWalking 链路追踪 |

**扩容要点**：应用服务保持无状态，会话统一存 Redis；书籍详情与章节列表走缓存；MySQL 主从读写分离，热点数据预热。

---

## 三、后端框架结构（Java / Spring Boot 3）

### 技术选型

`Spring Boot 3.2` · `Spring Cloud Alibaba` · `Spring Security + JWT` · `MyBatis-Plus 3.5` · `MySQL 8.0`  
`Redis 7 + Redisson` · `RocketMQ 5.0` · `Elasticsearch 8` · `Nacos 2.3` · `Sentinel 1.8`

### 分层架构（单服务内部）

```
Controller 层  →  Service 层  →  Mapper 层  →  存储与中间件
参数校验 · 统一响应 · 鉴权注解
业务编排 · 事务控制 · 缓存策略
MyBatis-Plus · 分页 · 多数据源
MySQL · Redis · ES · MQ
```

### 工程模块结构（Maven 多模块，建议）

```
moyue-parent           父工程：版本与依赖 BOM 统一管理
├── moyue-common       通用工具 / 常量 / 异常 / 统一响应体 R<T>
├── moyue-api          Feign 接口定义 + DTO
├── moyue-gateway      网关：路由 / 鉴权 / 限流 / 日志
├── moyue-auth         认证中心：登录 / 签发与校验 JWT
├── moyue-job          定时任务：XXL-Job 调度
└── moyue-service-*    业务服务：user / book / read / chapter / author /
                       comment / audit / operation / message / stat
```

### 关键设计要点

- 统一响应体 `R<T>` + 全局异常处理
- 接口幂等 + Redisson 分布式锁
- 缓存穿透 / 击穿 / 雪崩防护
- 分布式事务：本地消息表 + MQ 最终一致
- 定时任务 XXL-Job 分片执行
- 全链路 traceId 埋点 + 日志采集

---

## 四、前端框架结构（Vue 3）

### 技术选型

`Vue 3.4` · `Vite 5` · `TypeScript 5` · `Pinia 2` · `Vue Router 4`  
`Element Plus` · `Axios` · `ECharts 5` · `UnoCSS` · `Vitest`

### 工程目录结构

```
moyue-web/                   Vue 3 + Vite + TypeScript 工程
  src/api/                   接口封装 · Axios 实例 · 模块化 API
  src/assets/                静态资源（图片 / 字体 / 图标）
  src/components/            通用组件（书籍卡片 / 分页 / 评分 / 空状态）
  src/composables/           组合式函数（分页 / 鉴权 / 主题 / 埋点）
  src/layout/                三端布局（Reader / Author / Admin）
  src/router/                路由表 · 权限守卫 · 动态菜单
  src/stores/                Pinia 状态（user / book / read）
  src/styles/                主题变量（绯红 / 金 / 纸底 / 墨色）
  src/utils/                 request · format · storage · permission
  src/views/reader/          首页 / 分类 / 排行 / 书籍详情 / 阅读器 / 书架
  src/views/author/          工作台 / 作品 / 章节 / 数据 / 稿酬 / 互动
  src/views/admin/           数据概览 / 评论审核 / 举报处理 / 用户管理 / 运营公告
```

### 工程约定

- 主题令牌：CSS Variables 统一颜色 / 字号 / 圆角
- 权限控制：路由级 + 按钮级（`v-permission` 指令）
- 性能：路由懒加载 · 组件异步 · 图片懒加载
- 埋点：曝光 / 点击统一上报（`useTracker`）
- 响应式：桌面 1440 优先，移动端 390 断点
- 接口约定：统一响应体 `R<T>` · 错误码集中管理

---

## 五、部署与运维架构

### 部署拓扑

```
用户终端 › CDN 加速 › Nginx 反向代理 › Spring Cloud Gateway
        › Kubernetes 集群（应用服务多副本）› 中间件 / 数据层
```

### 环境与发布流水线

| 环境      | 定位                         |
| ------- | -------------------------- |
| dev     | 本地联调 · Docker Compose 一键启动 |
| test    | 自动化测试 · 接口 Mock · 造数脚本     |
| staging | 灰度验证 · 数据脱敏 · 容量压测         |
| prod    | 多可用区 · 蓝绿 / 滚动发布           |

流水线：`Git 提交 › Jenkins 构建 › 镜像推送 › K8s 滚动更新`；回滚策略：保留近 5 个镜像版本 · 一键回滚。

### 监控与运维

- **指标**：Prometheus 采集 · Grafana 看板与告警
- **日志**：ELK 集中检索 · traceId 全链路关联
- **链路**：SkyWalking 调用链耗时与异常分析
- **弹性**：HPA 按 CPU / QPS 自动扩缩容
- **告警**：企业微信 / 邮件 / 短信分级触达
- **安全**：WAF · 接口签名 · 敏感字段加密

---

## 六、核心业务链路

**读者阅读链路**  
`客户端发起 › 网关鉴权 › 书城服务 › 阅读服务 › Redis 缓存 › MySQL 读库`

**作者发布章节链路**  
`作者提交 › 章节服务（审核中）› 消息队列 › 审核服务 › 自动发布 › 通知与缓存`

---

## 七、接口契约（OpenAPI 3.0）

**BasePath `/api/v1`**，分组：鉴权 / 书城 / 章节 / 互动 / 支付 / 管理。

### 契约约定

| 约定    | 说明                                             |
| ----- | ---------------------------------------------- |
| 统一响应体 | `R<T>`：`code` / `message` / `data` / `traceId` |
| 鉴权头   | `Authorization: Bearer {accessToken}`          |
| 分页    | 入参 `page` / `size`，出参 `total` + `records`      |
| 错误码   | 5 位业务码，HTTP 统一返回 200 承载                        |
| 幂等    | 写操作需带 `Idempotency-Key` 请求头                    |
| 版本    | 路径版本化，破坏性变更升版本                                 |


### 核心接口清单（20 个）

**用户与鉴权　user-service**

| 方法   | 路径                      | 说明                                       |
| ---- | ----------------------- | ---------------------------------------- |
| POST | `/api/v1/auth/login`    | 手机号 + 密码登录，返回 accessToken / refreshToken |
| POST | `/api/v1/auth/register` | 注册账号并下发短信验证码                             |
| POST | `/api/v1/auth/refresh`  | 用 refreshToken 换取新的 accessToken          |
| GET  | `/api/v1/users/me`      | 获取当前登录用户资料与角色权限                          |

**书城与检索　book-service**

| 方法   | 路径                       | 说明                          |
| ---- | ------------------------ | --------------------------- |
| GET  | `/api/v1/books`          | 分页查询：分类 / 标签 / 排序 / 状态筛选    |
| GET  | `/api/v1/books/{bookId}` | 书籍详情：简介 / 作者 / 统计 / 最新章节    |
| GET  | `/api/v1/books/search`   | 全文检索（Elasticsearch），返回书籍与作者 |
| POST | `/api/v1/shelf/{bookId}` | 加入或移出我的书架（幂等）               |

**章节与创作　chapter-service / author-service**

| 方法   | 路径                                       | 说明                      |
| ---- | ---------------------------------------- | ----------------------- |
| GET  | `/api/v1/books/{bookId}/catalog`         | 章节目录（分页，Redis 缓存 10 分钟） |
| GET  | `/api/v1/chapters/{chapterId}`           | 章节正文（热点缓存，未命中回源 MySQL）  |
| POST | `/api/v1/author/books/{bookId}/chapters` | 新建章节（默认草稿态，支持自动保存）      |
| POST | `/api/v1/author/chapters/{id}/publish`   | 提交发布 / 定时发布，进入审核队列      |

**互动与支付　comment-service / pay-service**

| 方法   | 路径                         | 说明                      |
| ---- | -------------------------- | ----------------------- |
| GET  | `/api/v1/comments`         | 评论列表：按书籍 / 章节分页，含点赞数    |
| POST | `/api/v1/comments`         | 发表评论，先落库待审再异步机审         |
| POST | `/api/v1/rewards`          | 创建打赏订单，返回 orderNo 与支付参数 |
| GET  | `/api/v1/orders/{orderNo}` | 轮询订单支付状态（幂等查询）          |

**管理与审核　admin-service / audit-service**

| 方法   | 路径                                  | 说明                       |
| ---- | ----------------------------------- | ------------------------ |
| GET  | `/api/v1/admin/audit/comments`      | 待审核评论列表（举报次数 / 机审分）      |
| POST | `/api/v1/admin/audit/comments/{id}` | 审核通过 / 驳回，写回审核结果与通知      |
| GET  | `/api/v1/admin/stats/overview`      | 运营数据总览：用户 / 作品 / 评论 / 收入 |
| POST | `/api/v1/admin/announcements`       | 发布运营公告，支持定时上线与置顶         |

### 统一响应体示例

```json
{
  "code": 0,
  "message": "success",
  "data": { "bookId": 10086, "title": "万古剑尊", "wordCount": 3280000 },
  "traceId": "a1b2c3d4e5f6789"
}
```

### 错误码约定

| 错误码   | 含义                |
| ----- | ----------------- |
| 0     | 请求成功              |
| 10001 | 参数校验失败            |
| 10002 | 未登录 / Token 失效    |
| 10003 | 无权限访问该资源          |
| 20001 | 资源不存在或已下架         |
| 30001 | 请求频率超限（Sentinel）  |
| 40001 | 服务内部异常（含 traceId） |
| 50001 | 支付失败 / 订单超时关闭     |

### Swagger / OpenAPI YAML 快照

画布第 05 节给出可直接落地的 YAML 骨架，完整版已落盘 **`deliverables/openapi.yaml`**（通过 YAML 语法校验：21 个路径 · 6 个分组 · 4 个 Schema）。

```yaml
openapi: 3.0.3
info:
  title: 墨阅小说网 API
  version: 1.0.0
servers:
  - url: https://api.moyue.com/api/v1
    description: 生产环境
tags:
  - name: auth      # 鉴权
  - name: book      # 书城
  - name: chapter   # 章节
  - name: comment   # 互动
  - name: reward    # 打赏支付
  - name: admin     # 管理后台
security:
  - bearerAuth: []
components:
  securitySchemes:
    bearerAuth: { type: http, scheme: bearer, bearerFormat: JWT }
  schemas:
    ApiResponse:   # code / message / data / traceId
    LoginReq:      # phone + password（含正则与长度校验）
    UserInfo:      # 用户资料与角色
    PageResult:    # total / page / size / records
```

**使用方式**：后端引入 `springdoc-openapi` 后，该文件即前后端联调与 Mock 的唯一事实来源；前端可据此生成 TypeScript 类型与 Axios 客户端。

---

## 八、数据库设计（ER 与核心表结构）

MySQL 8 · InnoDB · utf8mb4。字段标识：`PK` 主键 / `FK` 外键 / 普通字段。

### 1. user　用户表

| 字段       | 类型          | 说明                     |
| -------- | ----------- | ---------------------- |
| id       | bigint      | **PK** 用户主键（雪花 ID）     |
| phone    | varchar(11) | 手机号，唯一索引               |
| nickname | varchar(32) | 用户昵称                   |
| role     | tinyint     | 枚举：1 读者 / 2 作者 / 3 管理员 |
| status   | tinyint     | 状态：0 禁用 / 1 正常         |

### 2. book　作品表

| 字段          | 类型           | 说明               |
| ----------- | ------------ | ---------------- |
| id          | bigint       | **PK** 作品主键      |
| author_id   | bigint       | **FK** → user.id |
| title       | varchar(128) | 作品名称             |
| category_id | bigint       | **FK** 分类 ID     |
| status      | tinyint      | 连载 / 完结 / 下架     |

### 3. chapter　章节表

| 字段         | 类型           | 说明               |
| ---------- | ------------ | ---------------- |
| id         | bigint       | **PK** 章节主键      |
| book_id    | bigint       | **FK** → book.id |
| chapter_no | int          | 章节序号（联合唯一）       |
| title      | varchar(128) | 章节标题             |
| status     | tinyint      | 草稿 / 审核中 / 已发布   |

### 4. comment　评论表

| 字段      | 类型            | 说明               |
| ------- | ------------- | ---------------- |
| id      | bigint        | **PK** 评论主键      |
| user_id | bigint        | **FK** → user.id |
| book_id | bigint        | **FK** → book.id |
| content | varchar(1000) | 评论内容             |
| status  | tinyint       | 待审 / 通过 / 驳回     |

### 5. reward_order　打赏订单表

| 字段       | 类型            | 说明                   |
| -------- | ------------- | -------------------- |
| id       | bigint        | **PK** 订单主键          |
| order_no | varchar(32)   | 订单号，唯一索引             |
| user_id  | bigint        | **FK** → user.id　打赏人 |
| book_id  | bigint        | **FK** → book.id　作品  |
| amount   | decimal(10,2) | 打赏金额（元）              |

### 6. bookshelf　书架表

| 字段              | 类型       | 说明                  |
| --------------- | -------- | ------------------- |
| id              | bigint   | **PK** 书架记录主键       |
| user_id         | bigint   | **FK** → user.id　读者 |
| book_id         | bigint   | **FK** → book.id　书籍 |
| last_chapter_id | bigint   | **FK** 最后阅读章节       |
| add_time        | datetime | 加入书架时间              |

### 实体关系

```
user 1 : N book            作者拥有多部作品
book 1 : N chapter         作品包含章节
user 1 : N comment         用户发表评论
book 1 : N comment         作品下的评论
user 1 : N reward_order    用户发起打赏
user N : N book            经 bookshelf 关联
```

### 索引与规范

- 联合唯一索引：`chapter（book_id, chapter_no）`
- 唯一索引：`user.phone` · `reward_order.order_no`
- 查询索引：`comment（book_id, status）` · `book（category_id）`
- 主键统一雪花 ID（bigint），不暴露业务量
- 时间字段统一 `create_time` / `update_time`（datetime）
- 逻辑删除：`is_deleted` tinyint(1) 默认 0

### 完整建表脚本（MySQL 8 DDL）

画布第 04 节给出可直接执行的核心建表语句，完整版已落盘 **`deliverables/moyue-schema.sql`**（169 行）：

| 项      | 数量   | 说明                                                        |
| ------ | ---- | --------------------------------------------------------- |
| 数据表    | 8 张  | `user` `book` `chapter` `comment` `reward_order` `bookshelf` `author_income` `audit_task` |
| 唯一约束   | 4 个  | `uk_phone` · `uk_book_no` · `uk_order_no` · `uk_user_book`     |
| 二级索引   | 13 个 | 覆盖作者 / 分类 / 状态 / 书籍-状态 / 结算月等查询路径                           |
| 字段注释   | 78 条 | 每个字段均带 `COMMENT`，枚举值含义直接写在注释里                              |

脚本统一 `ENGINE=InnoDB DEFAULT CHARSET=utf8mb4`、主键 `BIGINT` 雪花 ID、时间字段 `create_time` / `update_time` 自动维护、逻辑删除 `is_deleted`。比画布多出的两张表（`author_income` 稿酬结算、`audit_task` 审核任务）用于支撑发布链路与稿酬链路。

---

## 九、核心时序图

### 1. 登录鉴权时序

参与者：客户端（Vue 3）· API 网关 · 用户服务 · Redis · MySQL

| #  | 调用           | 说明                                 |
| -- | ------------ | ---------------------------------- |
| 1  | 客户端 → 网关     | `POST /api/v1/auth/login`　手机号 + 密码 |
| 2  | 网关 → 用户服务    | 路由转发 · JWT 过滤器放行                   |
| 3  | 用户服务 → MySQL | 按 phone 查询用户与密码哈希                  |
| 4  | MySQL ⇢ 用户服务 | 返回 user 记录                         |
| 5  | 用户服务 → Redis | 校验密码后写入 refreshToken（TTL 7 天）      |
| 6  | Redis ⇢ 用户服务 | 缓存写入成功                             |
| 7  | 用户服务 ⇢ 网关    | 返回 accessToken / refreshToken      |
| 8  | 网关 ⇢ 客户端     | `200 R<T>`　登录成功（写入本地 Pinia）        |
| 9  | 客户端 → 网关     | 携带 Authorization 访问业务接口            |
| 10 | 网关 → Redis   | 校验 token 有效性（本地缓存比对）               |
| 11 | Redis ⇢ 网关   | 有效 → 放行并注入用户上下文                    |
| 12 | 网关 ⇢ 客户端     | 返回业务数据                             |

### 2. 打赏支付时序

参与者：读者端（Vue 3）· API 网关 · 支付服务 · 微信支付平台 · MySQL / RocketMQ

| #  | 调用          | 说明                          |
| -- | ----------- | --------------------------- |
| 1  | 读者端 → 网关    | `POST /api/v1/rewards`　发起打赏 |
| 2  | 网关 → 支付服务   | 鉴权 + 限流，转发下单请求              |
| 3  | 支付服务 → 存储   | 创建订单（status = 待支付）          |
| 4  | 存储 ⇢ 支付服务   | 返回 orderNo                  |
| 5  | 支付服务 → 微信支付 | 统一下单，获取支付参数                 |
| 6  | 微信支付 ⇢ 支付服务 | 返回 prepay_id                |
| 7  | 支付服务 ⇢ 网关   | 返回支付参数（prepay_id 等）         |
| 8  | 网关 ⇢ 读者端    | 唤起收银台（H5 / JSAPI）           |
| 9  | 微信支付 → 网关   | 异步回调 notify（验签 + 解密）        |
| 10 | 网关 → 支付服务   | 回调处理（幂等校验）                  |
| 11 | 支付服务 → 存储   | 更新订单为已支付 + 投递 MQ            |
| 12 | 支付服务 ⇢ 微信支付 | 返回 SUCCESS（避免重复回调）          |

### 3. 章节审核与自动发布时序

参与者：作者端（Vue 3）· 章节服务 · RocketMQ · 审核服务 · XXL-Job / MySQL

| #  | 调用            | 说明                                        |
| -- | ------------- | ----------------------------------------- |
| 1  | 作者端 → 章节服务    | 提交章节　`POST /author/chapters/{id}/publish` |
| 2  | 章节服务 → 存储     | 落库 status = 待审核 · 同事务写本地消息表               |
| 3  | 存储 ⇢ 章节服务     | 事务提交成功                                    |
| 4  | 章节服务 → RocketMQ | 投递审核消息（chapterId + traceId）              |
| 5  | RocketMQ → 审核服务 | 消费消息 · 按 msgId 幂等去重                       |
| 6  | 审核服务 → 审核服务   | 机审规则命中 + 人工复核，写审核结论                       |
| 7  | 审核服务 ⇢ 章节服务   | 返回审核结论                                    |
| 8  | 审核服务 → 存储     | 写回 status：已发布 / 驳回                        |
| 9  | XXL-Job → 章节服务 | 到点触发定时发布                                  |
| 10 | 章节服务 → 存储     | 更新书籍字数 · 失效章节缓存                           |
| 11 | RocketMQ → 审核服务 | 审核失败重试最多 3 次，超限转死信人工介入                    |
| 12 | 章节服务 ⇢ 作者端    | 返回 `R<T>` · 站内信通知作者审核结果                   |

### 4. 敏感评论机审时序

参与者：读者端（Vue 3）· 互动服务 · 内容安全引擎（机审）· RocketMQ · 管理后台人工复核

| #  | 调用            | 说明                                |
| -- | ------------- | --------------------------------- |
| 1  | 读者端 → 互动服务    | `POST /api/v1/comments`　提交评论      |
| 2  | 互动服务 → 机审引擎   | 机审：敏感词库 + 模型打分（score 0-1）         |
| 3  | 机审引擎 ⇢ 互动服务   | 返回 score 与风险标签（涉政 / 暴力 / 广告）      |
| 4  | 互动服务 → RocketMQ | 评论落库 status = 待审 · 投递异步消息         |
| 5  | RocketMQ → 管理后台 | 低分评论推送人工复核队列                      |
| 6  | 管理后台 → 互动服务   | 拉取待复核评论列表                         |
| 7  | RocketMQ → 互动服务 | 消费消息：自动通过并刷新缓存                    |
| 8  | 互动服务 ⇢ 读者端    | 返回 `R<T>`：评论已提交，审核中              |
| 9  | 管理后台 → 互动服务   | 人工判定：通过 / 删除 / 封禁账号               |
| 10 | 管理后台 → 作者     | 站内信通知评论作者处理结果                     |
| 11 | 管理后台 → 存储     | 写审计日志（操作人 · 时间 · 结论）              |
| 12 | 机审引擎 → RocketMQ | 机审超时或失败重试，超限转死信告警                 |

### 时序要点

- **登录**：accessToken 短时效（2h）+ refreshToken 长时效（7d），鉴权走 Redis 避免每请求打库
- **支付**：下单先落库再调支付平台；回调必须验签 + 幂等（orderNo 唯一）；成功后投递 MQ 异步发放打赏与稿酬记账
- **章节审核**：提交即落库为待审核，本地消息表 + MQ 保证不丢；审核写回后统一失效缓存；XXL-Job 到点自动发布
- **评论机审**：机审同步打分为主（超时可降级为待审）；低分转人工复核；结果异步回写；失败重试超限转死信
- **通用**：跨服务调用统一携带 traceId；网关集中限流熔断；写操作带幂等键防重；失败重试走 MQ 死信兜底

---

## 十、视觉与规范

- 沿用「墨阅小说网」设计体系：绯红 `#C9382E`、金 `#E8A33D`、暖纸底 `#F7F5F1`、墨色 `#26221E`
- 层级色板（用于区分架构层）：客户端=金 / 接入=绯红 / 应用服务=深绯 / 中间件=青绿 / 数据=墨蓝 / 基础设施=暖灰
- 品牌字：思源宋体 Bold；正文与代码标识：Sarasa Gothic SC
- 时序图规范：实线=请求、虚线=响应；参与者用层级色板编码；生命线为虚线
- **文本节点不含 emoji / Unicode 图形字符**：禁 `U+2300–U+27BF`、`U+2600–U+27BF`、`U+1F300–U+1FAFF`；箭头类（`← → ↑ ↓ ▼`）与圈号 / 几何符（`①` `●` `○`）同样不用。视觉符号一律用独立 `frame + svg` 节点，或改写为纯文本（`[1]`…`[12]`、`PK` / `FK`、`1 : N`）
- 已同步清理界面设计稿（`file/724052320918211`）「状态与反馈规范」「阅读器」「章节管理 / 数据统计 / 帮助中心」画布中的同类字符，共替换 **13 处图标 + 11 处文本**

## 十一、交付产物清单

| 产物                | 路径                                            | 说明                                            |
| ----------------- | --------------------------------------------- | --------------------------------------------- |
| **架构全集 PDF**      | `deliverables/arch/系统架构设计-全集.pdf`            | 8 块画布合并（约 4.8 MB），评审 / 方案附件用                     |
| **单块 PNG（2x）**    | `deliverables/arch/01~08-*.png`               | 8 张高清图，方案文档插图用                                 |
| **MySQL 建表脚本**    | `deliverables/moyue-schema.sql`               | 8 张表 · 4 唯一约束 · 13 索引 · 78 字段注释，可直接执行          |
| **OpenAPI 契约**    | `deliverables/openapi.yaml`                   | 21 路径 · 6 分组 · 4 Schema，已通过 YAML 语法校验         |
| **本说明**           | `系统架构设计说明.md`                                 | 架构 / 接口 / 库表 / 时序的完整文字版，含**第十四~十六节**后端现状盘点与路线图   |
| **界面稿总览**         | `overview.md`                                 | 26 块界面画布清单                                    |
| **后端工程**         | `moyue-parent/`                               | 18 个 Maven 模块 · 39 个 REST 端点 + 1 个 WS 端点 · 18 张表（Flyway V1–V4） |
| **一键脚本**         | `moyue-parent/scripts/`                       | `build.sh`（构建）/ `start-all.sh`（按序启动）/ `smoke-test.sh`（全链路断言） |
| **后端说明**         | `moyue-parent/README.md`                      | 模块表 / 端口 / 启动顺序 / 全链路 curl / 已知遗留        |

划线导出产物对应画布节点：`3:1` 总体架构 · `3:116` 后端 · `3:181` 前端 · `3:252` 部署 · `3:319` 链路 · `3:364` 接口 · `3:510` 数据库 · `3:591` 时序。

## 十二、校验结论

8 块架构画布全部通过 `capture_layout` 校验，**无真实布局缺陷**：

- 良性 `LARGE_EMPTY_AREA` 警告（页头标题短于容器宽度、卡片宽于短文），属正常设计形态
- 时序图中的「重叠」全部为**箭头与文字穿过生命线**——时序图固有画法，非缺陷

**交付文件校验**：

- `moyue-schema.sql`：8 张 `CREATE TABLE` 齐备，索引与注释完整
- `openapi.yaml`：`yaml.safe_load` 解析通过（`openapi 3.0.3` / 21 paths / 6 tags / 4 schemas）
- 导出 PDF（`%PDF-` 头）与 8 张 PNG（`89504E47` 头）签名合法、文件完整
- **禁用字符清零**：两份设计文件内的 `U+2300–U+27BF` 及箭头类字符（`✓ ✕ ◌ ↓ ▼ ← → ↑ ◌ ● ○`）已全部替换为独立 SVG 图标节点或纯文本写法

## 十三、下一步建议（含落地状态更新）

> 原第 1 条「落地代码工程脚手架」**已完成**，工程实际规模超出原计划，现更新状态并保留其余建议。
> 完整的现状盘点 / 延伸规划 / 待完善清单见 **十四、十五、十六** 三节。

| # | 原建议 | 状态 |
| --- | --- | --- |
| 1 | 落地代码工程脚手架（Maven 多模块 + Vue3） | **[已完成]** 18 个 Maven 模块 / 18 张表 / 39 个 REST 端点，规模超出原「网关+鉴权+一个示例服务」计划；详见第十四节 |
| 2 | 接入契约工具链（`springdoc-openapi` + 前端代码生成） | **[未完成]** `openapi.yaml` 仍为设计期产物，未与实现自动同步；见 16-8 |
| 3 | 补 DDL 迁移工具（Flyway / Liquibase） | **[已完成]** 已接入 Flyway，迁移落在 `moyue-common`，V1/V2/V3/V4 四份，全服务共享 |
| 4 | 补其余时序（稿酬结算 / 书架同步 / 搜索推荐） | **[未完成]** 对应业务尚未实现，见第十五节 P1-7、P2-1 |
| 5 | 界面稿画布按 SVG 图标规范复核 | **[部分完成]** 界面稿已做禁用字符清零，但 52 块画布对应的前端页面绝大多数未实现；见 16-12 |

---

## 十四、现有功能全景（后端已实现）

> 本节数据均从工程源码静态盘点得出（2026-09-12），非计划值。

### 14.1 工程规模一览

| 维度 | 数量 | 说明 |
| --- | --- | --- |
| Maven 模块 | **18** | parent / common / api / gateway / job + 14 个业务服务 |
| 对外服务端口 | **8080–8095** | 网关 8080，业务服务 8081–8095，XXL-Job RPC 9099 |
| REST 端点 | **39** | 分布在 14 个业务服务；均为 `/api/v1` 前缀 |
| 实时端点 | **1** | WebSocket `/ws/im`（moyue-im，8093） |
| 数据库表 | **18** | V1 建 8 表 + V3 建 1 表 + V4 建 9 表 |
| Flyway 迁移 | **4** | V1 初始化 / V2 书籍种子 / V3 站内信 / V4 IM+积分+博客 |
| 跨服务 Feign 客户端 | **7** | User / Book / Chapter / Comment / Im / Points / Blog |
| 共享 DTO | **11** | User / BookSummary / Chapter / Comment / Conversation / Message / PointsAccount / PointsProduct / PointsOrder / BlogPost / BlogComment |
| XXL-Job 任务 | **3** | demoJobHandler / shardingJobHandler / statAggregateJobHandler |
| 前端页面 | **4** | Login / BookList / DefaultLayout / App（远滞后于 52 块界面稿） |

### 14.2 平台与身份

| 服务 / 端口 | 方法 | 路径 | 功能 |
| --- | --- | --- | --- |
| auth 8081 | POST | `/api/v1/auth/login` | 登录，返回 accessToken + refreshToken |
| auth 8081 | POST | `/api/v1/auth/register` | 注册，BCrypt 加密写库 |
| auth 8081 | POST | `/api/v1/auth/refresh` | 刷新令牌（refreshToken 存 Redis） |
| auth 8081 | GET | `/api/v1/users/me` | 读网关注入的 `X-User-Id` 取当前用户资料 |
| user 8083 | GET | `/api/v1/users/{id}` | 用户资料查询 |
| user 8083 | GET | `/api/v1/users` | 用户分页列表 |

### 14.3 内容消费

| 服务 / 端口 | 方法 | 路径 | 功能 |
| --- | --- | --- | --- |
| book 8082 | GET | `/api/v1/books` | 书籍分页（经 Feign 解析作者昵称） |
| book 8082 | GET | `/api/v1/books/{bookId}` | 书籍详情 |
| chapter 8085 | GET | `/api/v1/chapters/{chapterId}` | 章节详情 |
| chapter 8085 | GET | `/api/v1/chapters` | 章节目录分页 |
| read 8084 | GET | `/api/v1/read/bookshelf/{userId}` | 查询书架 |

> **重要**：消费域三个服务**全部只有读接口**，没有任何写接口——详见第十六节 16-3。

### 14.4 互动（评论 / IM / 博客）

| 服务 / 端口 | 方法 | 路径 | 功能 |
| --- | --- | --- | --- |
| comment 8087 | GET | `/api/v1/comments` | 评论查询（按 bookId） |
| comment 8087 | POST | `/api/v1/comments` | 发表评论 |
| im 8093 | POST | `/api/v1/im/conversations` | 创建会话（单聊 type=1 / 群聊 type=2） |
| im 8093 | GET | `/api/v1/im/conversations` | 我的会话列表（分页） |
| im 8093 | GET | `/api/v1/im/conversations/{conversationId}` | 会话详情（聚合成员 ID） |
| im 8093 | GET | `/api/v1/im/conversations/{conversationId}/messages` | 会话消息分页 |
| im 8093 | POST | `/api/v1/im/conversations/{conversationId}/messages` | 发送消息（落库 + WS 广播） |
| blog 8095 | POST | `/api/v1/blog/posts` | 发布文章 |
| blog 8095 | GET | `/api/v1/blog/posts` | 文章列表（authorId 可选） |
| blog 8095 | GET | `/api/v1/blog/posts/{id}` | 文章详情（view_count 自增） |
| blog 8095 | PUT | `/api/v1/blog/posts/{id}` | 编辑文章 |
| blog 8095 | POST | `/api/v1/blog/posts/{id}/comments` | 评论文章 |
| blog 8095 | GET | `/api/v1/blog/posts/{id}/comments` | 文章评论列表 |
| blog 8095 | POST | `/api/v1/blog/posts/{id}/like` | 点赞 / 取消点赞（幂等切换） |
| blog 8095 | DELETE | `/api/v1/blog/posts/{id}` | 删除文章（逻辑删除） |

### 14.5 权益与作者

| 服务 / 端口 | 方法 | 路径 | 功能 |
| --- | --- | --- | --- |
| points 8094 | GET | `/api/v1/points/accounts/{userId}` | 积分账户（不存在自动初始化） |
| points 8094 | GET | `/api/v1/points/products` | 上架商品列表 |
| points 8094 | POST | `/api/v1/points/orders` | 兑换商品（事务：扣余额 + 扣库存 + 建单） |
| points 8094 | GET | `/api/v1/points/orders` | 我的兑换订单 |
| author 8086 | GET | `/api/v1/author/income/{authorId}` | 作者稿酬流水 |

### 14.6 消息与管理后台

| 服务 / 端口 | 方法 | 路径 | 功能 |
| --- | --- | --- | --- |
| message 8090 | GET | `/api/v1/messages/{userId}` | 站内通知查询 |
| message 8090 | POST | `/api/v1/messages` | 发送站内通知 |
| audit 8088 | GET | `/api/v1/admin/audit/comments` | 待审评论任务列表 |
| operation 8089 | GET | `/api/v1/admin/announcements` | 运营公告列表 |
| stat 8091 | GET | `/api/v1/admin/stats/overview` | 全站聚合统计（只读） |
| points 8094 | POST | `/api/v1/admin/points/products` | 后台新增商品 |
| points 8094 | PUT | `/api/v1/admin/points/products/{id}` | 后台修改商品（上下架用 status） |
| points 8094 | GET | `/api/v1/admin/points/products` | 后台商品列表 |

### 14.7 平台级基础能力

| 能力 | 实现情况 |
| --- | --- |
| 统一响应体 `R<T>` | 已实现（`code` / `message` / `data` / `traceId`），HTTP 统一 200 承载 |
| 统一错误码 | `ResultCode` 定义 0 / 10001 / 10002 / 10003 / 20001 / 30001 / 40001 / 50001（部分未落地，见 16-7） |
| 网关鉴权 | `JwtAuthGlobalFilter` 校验 Bearer Token，注入 `X-User-Id` / `X-User-Role` |
| 服务发现 | Nacos，`lb://moyue-<svc>` 显式路由，`discovery.locator.enabled=false` |
| 数据访问 | MyBatis-Plus 3.5.7，雪花 ID；全局逻辑删除字段 `is_deleted`；分页拦截器 |
| 数据库迁移 | Flyway 共享 `classpath:db/migration`，`baseline-on-migrate` |
| 服务间调用 | OpenFeign + Loadbalancer，返回 `R<DTO>`；昵称解析失败安全降级 |
| 定时任务 | XXL-Job 2.4.0 执行器，3 个 Handler |
| 实时推送 | WebSocket `/ws/im`，按 userId 维护会话表，消息落库后广播；失败仅记日志不阻断 HTTP |
| 幂等设计 | 博客点赞唯一键 `uk_post_user` + `DuplicateKeyException` 后 `revive()` 复活旧记录 |

---

## 十五、待延伸功能（路线图）

> 按优先级排列。P0 = 不补则工程不可用；P1 = 业务链路断点；P2 = 规模化与体验。

### P0 — 打通工程闭环

| # | 功能 | 说明与落地指向 |
| --- | --- | --- |
| 1 | **实机编译验证与修复** | 当前全部代码未经编译。首要动作：`mvn clean package -DskipTests`，修复暴露的编译错误 |
| 2 | **补充自动化测试** | 现有构建全程 `-DskipTests`，零测试资产。至少补齐积分兑换、博客点赞、IM 收发三条主链路的集成测试 |
| 3 | **契约与 DDL 同步** | `openapi.yaml`（21 paths）→ 同步至实际 39 端点；`moyue-schema.sql`（8 表）→ 同步至实际 18 表 |
| 4 | **前端页面补齐** | 现有 4 个 `.vue`（Login / BookList）。按优先级补：书籍详情 / 阅读器 / 书架 / 个人中心 |

### P1 — 补业务链路断点

| # | 功能 | 目标模块 | 说明 |
| --- | --- | --- | --- |
| 5 | **作者写作链路** | moyue-chapter | 当前仅有 2 个 GET。需补：草稿箱、创建 / 编辑 / 删除章节、定时发布、章节排序 |
| 6 | **作品管理** | moyue-book | 当前仅 2 个 GET。需补：创建作品、编辑、上架 / 下架、封面上传、作品删除与完结申请 |
| 7 | **打赏与支付** | moyue-operation（新或改） | `reward_order` 表已存在但无下单接口。需补：创建打赏订单、支付方式接入、打赏记录、退款、作者结算到账 |
| 8 | **写操作全面补齐** | read / audit / operation / comment / user | 书架增删、审核通过 / 驳回、公告增删、评论删除与点赞、用户资料更新——目前均无对应写接口 |
| 9 | **权限与角色（RBAC）** | 网关 + 各服务 | `/admin/**` 端点当前无角色校验。需建角色权限模型并在服务内做 `X-User-Role` 断言 |
| 10 | **积分获取渠道** | moyue-points | 只有消耗没有获取。需补：签到、阅读时长奖励、评论奖励等发放接口与流水 |
| 11 | **IM 能力增强** | moyue-im | 消息撤回、已读回执（`last_read_message_id` 已有字段未用）、群成员管理、图片 / 富文本消息 |
| 12 | **后台 CRUD 闭环** | audit / operation / stat | 审核操作、公告发布、统计维度下钻（作者维度、留存漏斗、明细导出） |

### P2 — 规模化与体验

| # | 功能 | 目标模块 | 说明 |
| --- | --- | --- | --- |
| 13 | **搜索与推荐** | 新建 moyue-search | 关键词检索、分类筛选、排序、热门推荐位；远期接入 Elasticsearch |
| 14 | **消息触达体系** | moyue-message | 站内信之外补邮件 / 短信 / App 推送，与 XXL-Job 定时任务联动 |
| 15 | **内容安全** | 新建或并入 audit | 敏感词过滤、机审（可对接做得更早的风控流水线）、举报处理闭环 |
| 16 | **缓存与性能优化** | 全服务 | Redis 缓存热点书籍 / 章节 / 列表，限流、熔断降级 |
| 17 | **多环境与配置治理** | 全服务 | dev / test / prod 隔离，密钥与配置迁入 Nacos Config，移除硬编码 JWT 密钥 |

---

## 十六、待完善问题清单（已识别的技术债与风险）

> 严重程度说明：**高危** = 会导致线上资损或不可用；**高** = 功能缺陷或安全 风险；**中** = 工程质量债；**低** = 体验与规范。

| # | 严重度 | 问题 | 位置 / 证据 | 建议修法 |
| --- | --- | --- | --- | --- |
| 16-1 | ~~**高危**~~ **[已修复]** | **积分兑换并发超卖**：原实现先 `SELECT` 校验库存与余额，再在 Java 内算好后用绝对值 `updateById` 回写，事务内无行锁 / 乐观锁 / 原子 UPDATE。并发请求可同时读到 `stock=1` 并各自回写成 0 → 超卖；余额同理丢失一次扣减 | `moyue-points` `PointsService.createOrder` | **已改为数据库层条件原子更新**：库存 `setSql("stock = stock - 1, update_time = NOW()")` + 条件 `id=? AND status=1 AND stock>0`；余额 `setSql("balance = balance - X, total_spent = total_spent + X, ...")` + 条件 `user_id=? AND balance>=X`；均以 UPDATE 影响行数判定成败，为 0 则抛 `PARAM_ERROR`。`cost` 先做 null / 负值校验再拼接（`int` 只能是数字字面量，无注入面）。错误码与文案未变，脚本断言不受影响 |
| 16-2 | **高危** | **全工程未经编译验证**：开发环境无 JDK / Maven / Docker，18 个模块全部依赖静态核对，存在编译期 / 运行期缺陷未被发现的可能 | 全工程 | 首次交付前必须执行 `mvn clean package`，逐个模块排错；优先验证 `moyue-im`（本轮新增 WebSocket 改动最大） |
| 16-3 | **高** | **消费域零写接口**：book / chapter / read 三个服务合计 5 个端点全部是 GET，作者无法创作、读者无法加书架——读了半天不能写 | `BookController` / `ChapterController` / `ReadController` | 见 P1-5 / P1-6 / P1-8，按面向写的 curd 补齐 |
| 16-4 | **高** | **WebSocket 无法经网关**：`JwtAuthGlobalFilter` 是 `GlobalFilter`，对所有路径生效，WS 握手不带 Bearer 会被判 10002，客户端必须先绕过网关直连 8093 | 网关 `JwtAuthGlobalFilter` + `moyue-im` `WebSocketConfig` | 二选一：网关新增 ws 路由并把 `/ws/**` 加入白名单，或在网关过滤器中放行 Upgrade 请求；README 需明确标注，否则联调必踩 |
| 16-5 | **高** | **WS 会话状态存在本地内存**：`ImWebSocketHandler` 用 `ConcurrentHashMap` 维护 userId→sessions，多实例部署时消息只能推送到持有该连接的实例 | `moyue-im` `ImWebSocketHandler` | 引入 Redis Pub/Sub 或消息 broker 做跨实例广播；单点演示可接受，上生产前必须改 |
| 16-6 | **高** | **管理端点无越权防护**：`/admin/**` 仅靠网关注入 `X-User-Role`，服务内未做任何角色断言，普通用户拿到有效 Token 即可访问后台数据 | audit / operation / stat / points admin 端点 | 在服务侧新增切面或拦截器校验 `X-User-Role`，或网关注入前先校验角色白名单 |
| 16-7 | **中** | **错误码已定义但未实现**：30001 频率超限、50001 支付失败在 `ResultCode` 中占位，但无限流组件、无支付实现 | `moyue-common` `ResultCode` | 网关接入 Redis 限流；支付在 P1-7 中一并落地 |
| 16-8 | **中** | **契约与实现漂移**：`openapi.yaml` 21 paths vs 实际 39 端点；`moyue-schema.sql` 8 表 vs 实际 18 表；文档里的「核心接口清单（20 个）」已过时 | `deliverables/openapi.yaml`、`deliverables/moyue-schema.sql`、本文档第七节 | 引入 `springdoc-openapi` 自动生成，或将两份文件纳入人工同步清单 |
| 16-9 | **中** | **零测试资产**：构建统一 `-DskipTests`，无单测 / 集成测试，重构无安全网 | 全工程 | 见 P0-2 |
| 16-10 | **中** | **演示环境耦合**：所有服务共用单库与单 Nacos 命名空间，无多环境隔离；JWT 密钥硬编码在 common 模块 | 各服务 `application.yml`、`moyue-common` | 按环境拆分 profile / namespace；配置迁入 Nacos Config，密钥改环境变量 |
| 16-11 | **低** | **Feign 返回类型未对齐**：客户端声明 `R<UserDTO>`，控制器实际返回 `R<UserEntity>`，目前靠 Jackson 忽略未知字段侥幸可用，字段调整时易静默漂移 | `moyue-api` `UserClient` 等 | 统一让控制器返回 DTO，或让 Feign 客户端返回 Entity，二选一对齐（建议前者） |
| 16-12 | **低** | **前端严重滞后**：52 块界面稿对应仅 4 个 `.vue` 文件（Login / BookList / DefaultLayout / App） | `moyue-web/src` | 见 P0-4，按业务优先级分批补齐 |
| 16-13 | **低** | **幂等与防重缺失**：下单、发帖、点赞等缺少幂等键，网络重试会产生重复数据（当前仅博客点赞依赖唯一键侥幸防重） | points / blog 等写接口 | 引入请求幂等键（Redis SETNX）或对关键业务加唯一业务键 |
| 16-14 | **低** | **任务执行器强依赖**：`moyue-job` 依赖 XXL-Job Admin（8088），未启动会持续产生连接错误日志，干扰排障 | `moyue-job` | 支持无调度中心降级运行，或调整重连与日志级别 |
| 16-15 | **中** | **积分账户首次初始化竞态**：同一用户首次并发兑换时，两个事务都可能执行 `accountMapper.insert`，主键 `user_id` 冲突导致其中一个事务失败 | `moyue-points` `PointsService.createOrder`（账户不存在则 insert 分支） | **[已修复]** `insert` 包 `try / catch (DuplicateKeyException)`，catch 后继续——冲突说明行已存在，后续余额条件 UPDATE 对已存在行同样生效；MySQL 唯一键冲突只回滚当前语句、不中止事务，故安全 |
| 16-16 | **低** | **WS 序列化依赖容器 ObjectMapper**：广播优先复用容器注入的 `ObjectMapper`，若将来有人注入未注册 JSR310 模块的自定义实例，序列化 `LocalDateTime` 会抛异常并被 catch 吞掉 → 广播静默 no-op，极难排查 | `moyue-im` `ImWebSocketHandler` | **[已修复]** 改为始终使用自建且已注册 `JavaTimeModule`、已关闭 `WRITE_DATES_AS_TIMESTAMPS` 的 `fallbackMapper`，与容器配置解耦 |
