# 时序图 · 搜索推荐

> 模块：`moyue-search`（`SearchController` / `SearchService` / `ChapterSearchService` / `RecommendService` / `SearchInternalController` / `ReindexService`）+ 内容域增量索引链路（`moyue-content` 的 `ChapterService` 经 `SearchIndexClient` Feign 写 ES）。
> 阅读说明：**以下全部为已读源码确认的真实代码路径**。索引库为 `moyue_book` / `moyue_chapter` / `moyue_qa`（IK 分词），均经 Spring Data Elasticsearch `ElasticsearchOperations` / Repository 访问。

## 真实路径一：书籍 / 章节检索 + 推荐（用户侧）

```mermaid
sequenceDiagram
    autonumber
    participant U as 用户客户端
    participant GW as 网关 moyue-gateway
    participant SC as SearchController<br/>(moyue-search)
    participant SS as SearchService
    participant CSS as ChapterSearchService
    participant RS as RecommendService
    participant EO as ElasticsearchOperations
    participant ES as ES(moyue_book /<br/>moyue_chapter, IK分词)

    U->>GW: GET /api/v1/search/books?keyword=&categoryId=&sort=
    GW->>SC: searchBooks(keyword,categoryId,sort,page,size)
    SC->>SS: search(keyword,categoryId,sort,page,size)
    SS->>SS: 构建 NativeQuery: multiMatch(title^3,authorName^2,<br/>categoryName,description) minimumShouldMatch=75%
    SS->>SS: filter: status∈{1连载,2已完结}(filter context, 可缓存)<br/>+ 可选 categoryId term
    SS->>EO: search(NativeQuery, BookDocument.class)
    EO->>ES: 全文检索(查询侧 ik_smart)
    ES-->>EO: SearchHits<BookDocument>
    EO-->>SS: hits
    SS-->>SC: PageResult<BookDocument>(relevance/hot/latest 排序)
    SC-->>GW: R.ok(...)
    GW-->>U: 书籍检索结果

    U->>GW: GET /api/v1/search/chapters?keyword=&bookId=
    GW->>SC: searchChapters(keyword,bookId,page,size)
    SC->>CSS: search(keyword,bookId,page,size)
    CSS->>CSS: 构建 NativeQuery: multiMatch(chapterTitle^2,content)<br/>filter: status=2 已发布 + 可选 bookId
    CSS->>CSS: withHighlightQuery(<em>标签, fragmentSize=150, noMatchSize=100)
    CSS->>EO: search(NativeQuery, ChapterDocument.class)
    EO->>ES: 章节全文检索 + 正文高亮
    ES-->>EO: SearchHits<ChapterDocument>(含 highlight)
    EO-->>CSS: hits
    CSS-->>SC: PageResult<ChapterSearchResultDTO>(含高亮片段)
    SC-->>GW: R.ok(...)
    GW-->>U: 章节检索结果(高亮)

    U->>GW: GET /api/v1/search/recommend?limit=&sort=
    GW->>SC: recommend(limit,sort)
    SC->>RS: recommend(limit,sort)
    RS->>RS: Criteria(status).in(1,2); sort=hot→hotScore desc / latest→updateTime desc
    RS->>EO: search(CriteriaQuery, BookDocument.class, TopN)
    EO->>ES: 取 status∈{1,2} TopN
    ES-->>EO: hits
    EO-->>RS: List<BookDocument>
    RS-->>SC: 推荐书籍列表
    SC-->>GW: R.ok(...)
    GW-->>U: 推荐位
```

## 真实路径二：内容发布 / 审核触发增量索引同步（internal API → ES）

> 关键点：`/api/v1/internal/**` 为**服务间内部端点，不经网关**，由 `moyue-content` 经 Nacos `lb://moyue-search` 直连 Feign 调用（`SearchIndexClient`）。

```mermaid
sequenceDiagram
    autonumber
    participant CS as ChapterService<br/>(moyue-content:8082)
    participant SIC as SearchIndexClient<br/>(Feign→moyue-search)
    participant IC as SearchInternalController<br/>(moyue-search)
    participant CS2 as ChapterSearchService
    participant CR as ChapterSearchRepository
    participant ES as ES(moyue_chapter, IK)

    Note over CS: 章节发布 / 审核通过 / 编辑直达已发布
    CS->>CS: publish()/auditChapter()/updateChapter()
    CS->>CS: syncChapterIndexIfPublished(e)
    alt e.status == 2 已发布
        CS->>CS: toIndexDto(e): 截断正文≤20000字 + BookClient解析书名
        CS->>SIC: indexChapter(ChapterIndexDTO)
        SIC->>IC: POST /api/v1/internal/search/chapters/_index (Feign name=moyue-search)
        IC->>IC: ChapterDocument.fromIndexDto(chapter)
        IC->>CS2: index(doc)
        CS2->>CR: save(doc)  [chapterId 为 _id, 覆盖更新, 幂等]
        CR->>ES: UPSERT moyue_chapter 文档
        ES-->>CR: 已写入
        CR-->>CS2: ChapterDocument
        CS2-->>IC: doc
        IC-->>SIC: R.ok(chapter)
        SIC-->>CS: 索引成功(失败仅 warn, 不阻断主链路)
    else 非已发布(草稿/驳回/定时未到点)
        CS->>CS: 不索引(直接返回)
    end

    Note over CS: 章节删除 / 逻辑删除
    CS->>CS: deleteChapter()/removeChapterIndex()
    CS->>SIC: removeChapter(chapterId)
    SIC->>IC: DELETE /api/v1/internal/search/chapters/{chapterId}
    IC->>CS2: remove(chapterId)
    CS2->>CR: deleteById(chapterId)
    CR->>ES: 物理删除 moyue_chapter 文档
```

> 真实方法名（已核实）：
> - 触发：`ChapterService.publish()` / `auditChapter()` / `updateChapter()` 末尾调用 `syncChapterIndexIfPublished(ChapterEntity e)`；仅 `e.getStatus()==2(已发布)` 时调 `searchIndexClient.indexChapter(toIndexDto(e))`，否则跳过。`searchIndexClient` 为 `@Autowired(required=false)`，search 未注册时安全降级（仅 warn）。
> - `toIndexDto`：正文超 20000 字符截断（`CONTENT_INDEX_MAX`），书名经 `BookClient.getBook` 解析（不可用时空串兜底）。
> - 接收端：`SearchInternalController.indexChapter(@RequestBody ChapterIndexDTO)` → `ChapterDocument.fromIndexDto` → `ChapterSearchService.index` → `chapterSearchRepository.save`（chapterId 为 ES `_id`，幂等覆盖）。
> - Feign 链路：`moyue-api-search.SearchIndexClient`（`@FeignClient(name="moyue-search")`），端点 `POST /api/v1/internal/search/chapters/_index`。书籍索引 `indexBook`、问答索引 `indexQa` 机制完全对称（分别来自 `moyue-content` 的书籍发布与 `moyue-ai` 的对话落库）。

## 真实路径三：管理端全量重建索引（ReindexService 经 Feign 分页拉取）

```mermaid
sequenceDiagram
    autonumber
    participant ADM as 管理端/调度
    participant RZ as ReindexService<br/>(moyue-search:8085)
    participant BC as BookClient (Feign→moyue-content)
    participant CC as ChapterClient (Feign→moyue-content)
    participant AC as AiClient (Feign→moyue-ai)
    participant SS as SearchService / ChapterSearchService
    participant ES as ES

    ADM->>RZ: reindex("book" | "chapter" | "qa")
    alt type = book
        loop page = 1..MAX_PAGES (size=100)
            RZ->>BC: pageBooks(page,100)
            BC-->>RZ: R<PageResult<BookIndexDTO>>
            RZ->>SS: index(BookDocument.fromIndexDto(dto))  [覆盖写, bookId=_id]
        end
    else type = chapter
        loop page = 1..MAX_PAGES
            RZ->>CC: pageChapters(page,100)
            CC-->>RZ: R<PageResult<ChapterIndexDTO>>
            RZ->>SS: chapterSearchRepository.save(ChapterDocument) [chapterId=_id]
        end
    else type = qa
        loop page = 1..MAX_PAGES
            RZ->>AC: pageQa(page,100)
            AC-->>RZ: R<PageResult<QaIndexDTO>>
            RZ->>SS: qaSearchRepository.save(QaDocument) [messageId=_id]
        end
    end
    RZ-->>ADM: 重建文档总数(下游降级则抛 BizException 中止, 已写部分保留可重跑)
```

> 真实方法名（已核实）：`ReindexService.reindex(String type)` → `reindexBooks()` / `reindexChapters()` / `reindexQa()`，分别经 `BookClient.pageBooks` / `ChapterClient.pageChapters` / `AiClient.pageQa`（均为 `@FeignClient`，分页 size=100，最大 10000 页护栏）拉取全量索引载荷，逐条 `fromIndexDto` → `save` 覆盖写入对应索引（bookId/chapterId/messageId 为 `_id`，天然幂等）。下游 fallback（R.code=40002）或异常时抛 `BizException` 中止，已写入部分不受影响。

---

### Caption（关键说明 / 假设）

1. **索引库与分词**：`moyue_book` / `moyue_chapter` / `moyue_qa` 三套索引，均启用 IK 分词（查询侧 `ik_smart`）；书籍 `multi_match` 字段权重 `title^3 > authorName^2 > categoryName/description`，章节 `chapterTitle^2 > content`。状态白名单（书籍 `status∈{1,2}`、章节 `status=2`）置于 bool **filter context**（不计分且可被 ES 节点级 filter cache 复用）。
2. **内部端点不经网关**：`SearchInternalController` 的 `/api/v1/internal/search/**` 由内容域 / AI 域经 Nacos 服务名 `lb://moyue-search` **直连 Feign** 调用，不经 `moyue-gateway` 路由（与 `moyue-gateway` 路由表只暴露 `/api/v1/search/**`、`/api/v1/admin/search/**` 一致）。因此时序图二中 `SearchIndexClient → SearchInternalController` 之间无网关节点。
3. **高亮**：章节检索对 `content` 字段高亮，标签 `<em>`/`</em>`，`fragmentSize=150`、`noMatchSize=100`，经 `ChapterSearchResultDTO.from(hit)` 携带高亮片段返回。
4. **降级与幂等**：增量索引（路径二）Feign 失败仅记 warn、不阻断章节发布主流程；全量重建（路径三）下游不可用时中止但已写部分可重跑。所有索引写入以业务主键为 `_id`，重复推送覆盖更新（幂等），与删除（物理删 `_id`）配合保证最终一致。
5. **真实跨服务调用**：路径二 `ChapterService → SearchIndexClient`（moyue-search）；路径三 `ReindexService → BookClient / ChapterClient / AiClient`（moyue-content / moyue-ai）。其中 `BookClient`/`ChapterClient` 亦被内容域自身与 `moyue-system` 的 `RewardService`（解析书籍作者）复用。
6. **未涉及假设**：本图全部为现存代码路径，无假设环节。`SearchInternalController` 的书籍索引由 `moyue-content` 书籍发布/下架触发（与章节对称，机制同路径二），`indexQa` 由 `moyue-ai` 每轮对话落库后触发，文中以"机制对称"说明，未逐条展开。
